# Auto-generated — all validation content embedded for serverless deploy
METHODS = {
  "M01_Completeness/M01-E1_null_mandatory_fields": "# M01 \u2014 Completeness | Example 1: NULL in Mandatory Fields\n\n## The Question\n> You have just received a fresh load of customer data into the `Customers` table.\n> Before any downstream process can use it, your manager asks:\n> **\"Which records are missing critical contact information?\"**\n> Write a query that finds every row where Email, Phone, or DOB is NULL,\n> and produce a summary count by field so the data team can prioritise the fix.\n\n---\n\n## Table Reference\n**Customers** \u2014 `schemas/01_Customers.csv`\n\nKey columns for this check:\n\n| Column | Business Rule |\n|---|---|\n| Email | Mandatory \u2014 needed for all marketing and account comms |\n| Phone | Optional but flagged if missing |\n| DOB | Mandatory \u2014 needed for age verification |\n\n---\n\n## Solution Query\n\n```sql\n-- Part A: Row-level detail \u2014 who has a missing field?\nSELECT\n    CustomerID,\n    FirstName + ' ' + LastName        AS CustomerName,\n    CASE WHEN Email IS NULL THEN 'MISSING' ELSE 'OK' END AS Email_Status,\n    CASE WHEN Phone IS NULL THEN 'MISSING' ELSE 'OK' END AS Phone_Status,\n    CASE WHEN DOB   IS NULL THEN 'MISSING' ELSE 'OK' END AS DOB_Status,\n    CreatedDate\nFROM Customers\nWHERE Email IS NULL\n   OR Phone IS NULL\n   OR DOB   IS NULL\nORDER BY CreatedDate;\n\n-- Part B: Summary count by field \u2014 how bad is the problem?\nSELECT\n    'Email' AS FieldName,\n    COUNT(*) AS NullCount,\n    CAST(COUNT(*) * 100.0 / (SELECT COUNT(*) FROM Customers) AS DECIMAL(5,2)) AS NullPct\nFROM Customers WHERE Email IS NULL\nUNION ALL\nSELECT 'Phone', COUNT(*),\n    CAST(COUNT(*) * 100.0 / (SELECT COUNT(*) FROM Customers) AS DECIMAL(5,2))\nFROM Customers WHERE Phone IS NULL\nUNION ALL\nSELECT 'DOB', COUNT(*),\n    CAST(COUNT(*) * 100.0 / (SELECT COUNT(*) FROM Customers) AS DECIMAL(5,2))\nFROM Customers WHERE DOB IS NULL;\n```\n\n---\n\n## Expected Output\n\n**Part A (row detail):**\n\n| CustomerID | CustomerName | Email_Status | Phone_Status | DOB_Status |\n|---|---|---|---|---|\n| 3 | Mohammed Al-Rashid | MISSING | OK | OK |\n| 4 | Sara O'Brien | OK | MISSING | OK |\n| 5 | Chen Wei | OK | OK | MISSING |\n| 12 | Ravi Kumar | MISSING | OK | OK |\n| 14 | Carlos Ruiz | OK | OK | MISSING |\n\n**Part B (field summary):**\n\n| FieldName | NullCount | NullPct |\n|---|---|---|\n| Email | 2 | 10.00% |\n| Phone | 2 | 10.00% |\n| DOB | 2 | 10.00% |\n\n---\n\n## Explanation\n\n### Why `IS NULL` and not `= NULL`\n`IS NULL` is the correct T-SQL check for a missing value. You cannot use `= NULL` because\nNULL is not equal to anything \u2014 not even itself. Any comparison involving NULL evaluates\nto UNKNOWN, not TRUE or FALSE, so `WHERE Email = NULL` will never return rows.\n\n### Reading the field-level summary with UNION ALL\nThe `UNION ALL` pattern in Part B stacks three separate count queries into one result set.\nThis produces a field-level completeness report in a single query without repeating\na full table scan per field. Each SELECT block counts NULLs for one column; UNION ALL\nappends the rows without removing duplicates (using UNION instead would add overhead and\ncould merge identical counts incorrectly).\n\n### Forcing decimal in the percentage\nThe percentage calculation uses `COUNT(*) * 100.0` (note the `.0`) to force decimal\ndivision. Without it, SQL Server performs integer division and rounds everything to 0%\n\u2014 so 2 NULLs out of 20 rows would show as 0% instead of 10%.\n\n---\n\n## Interview Angle\n*\"How do you measure completeness in a data load?\"*\nAnswer: you check for NULLs in mandatory fields. You should also check for blank strings\n(see M01-E2) because systems sometimes write an empty string instead of a true NULL.\n\n---\n\n## Severity: **Critical**\nMissing mandatory fields block downstream processes \u2014 invoicing, communications, age-gating.\n",
  "M01_Completeness/M01-E2_blank_placeholder_strings": "# M01 \u2014 Completeness | Example 2: Blank Strings and Placeholder Values\n\n## The Question\n> After fixing the NULLs from Example 1, your QA lead points out something worse:\n> **\"Some rows have values, but those values are meaningless \u2014 they're placeholders like\n> 'N/A', 'Unknown', or empty strings.\"**\n> Write a query that detects blank strings AND common placeholder patterns across\n> the Customers table, and returns a cleaned flag column explaining what is wrong.\n\n---\n\n## Table Reference\n**Customers** \u2014 `schemas/01_Customers.csv`\n\nRow 16 has all fields set to \"N/A\".\nRow 19 has Phone = \"99999999999\" \u2014 clearly a fake placeholder.\nRow 7 has Email = \"tom.brooks\" \u2014 no domain, likely a partial placeholder.\n\n---\n\n## Solution Query\n\n```sql\n-- Detect blank strings AND common placeholder values\nSELECT\n    CustomerID,\n    FirstName,\n    LastName,\n    Email,\n    Phone,\n    CASE\n        WHEN Email IS NULL                       THEN 'NULL value'\n        WHEN LTRIM(RTRIM(Email)) = ''            THEN 'Blank string'\n        WHEN UPPER(Email) IN ('N/A','NA','NONE','UNKNOWN','NULL','TBC','TBD')\n                                                 THEN 'Placeholder text'\n        WHEN Email NOT LIKE '%@%.%'              THEN 'Invalid format (no @ or domain)'\n        ELSE 'OK'\n    END AS Email_Issue,\n    CASE\n        WHEN Phone IS NULL                       THEN 'NULL value'\n        WHEN LTRIM(RTRIM(Phone)) = ''            THEN 'Blank string'\n        WHEN UPPER(Phone) IN ('N/A','NA','NONE','UNKNOWN','NULL','TBC','TBD')\n                                                 THEN 'Placeholder text'\n        WHEN Phone LIKE '9999%'                  THEN 'Suspicious repeated digits'\n        ELSE 'OK'\n    END AS Phone_Issue\nFROM Customers\nWHERE\n    LTRIM(RTRIM(ISNULL(Email,''))) = ''\n    OR UPPER(ISNULL(Email,'')) IN ('N/A','NA','NONE','UNKNOWN','NULL')\n    OR Email NOT LIKE '%@%.%'\n    OR LTRIM(RTRIM(ISNULL(Phone,''))) = ''\n    OR UPPER(ISNULL(Phone,'')) IN ('N/A','NA','NONE','UNKNOWN','NULL')\n    OR Phone LIKE '9999%';\n```\n\n---\n\n## Expected Output\n\n| CustomerID | FirstName | Email | Email_Issue | Phone_Issue |\n|---|---|---|---|---|\n| 7 | Tom | tom.brooks | Invalid format (no @ or domain) | OK |\n| 16 | N/A | N/A | Placeholder text | Placeholder text |\n| 19 | Ahmed | ahmed.khan@email.com | OK | Suspicious repeated digits |\n\n---\n\n## Explanation\n\n### Stripping whitespace before checking for empty strings\n`LTRIM(RTRIM(value))` removes all leading and trailing spaces before the empty-string\ncomparison. Without it, a value stored as `\"   \"` (three spaces) would silently pass\nan `= ''` check on some systems, because the spaces technically make it non-empty.\nAlways trim before comparing to an empty string.\n\n### Combining NULL and blank into one check with ISNULL\n`ISNULL(column, '')` converts a NULL to an empty string before calling LTRIM/RTRIM.\nThis collapses the NULL case and the blank-string case into a single expression,\nso you can write one WHERE clause instead of chaining `IS NULL OR = ''` every time.\n\n### Building a shared placeholder blocklist\nThe `IN ('N/A','NA','NONE','UNKNOWN','NULL','TBC','TBD')` list should be agreed with\nthe business and data engineering teams. Different source systems have different\nconventions for \"I don't know this value.\" Build a central reference table of blocked\nplaceholder strings and reuse it across all validation scripts so the rule is\nmaintained in one place.\n\n---\n\n## Interview Angle\n*\"What is the difference between a NULL and a blank string in SQL?\"*\nA NULL means the value is unknown or absent. A blank string means the field was\npopulated with nothing \u2014 it EXISTS as a value, just an empty one. `IS NULL` will not\ncatch blank strings; `= ''` will not catch NULLs. You must check for both.\n\n---\n\n## Severity: **High**\nPlaceholder data silently passes NULL checks and corrupts downstream aggregations,\nemail sends, and customer lookups.\n",
  "M02_Uniqueness/M02-E1_duplicate_primary_key": "# M02 \u2014 Uniqueness | Example 1: Duplicate Primary Key / Exact Row Duplicates\n\n## The Question\n> During a data load audit, your team suspects that the `Customers` table has received\n> duplicate records from two source systems that were merged without deduplication.\n> **\"Find all duplicate customer records \u2014 first by exact key match, then by full-row\n> match \u2014 and return them ranked so we can identify which one to keep.\"**\n\n---\n\n## Table Reference\n**Customers** \u2014 `schemas/01_Customers.csv`\n\nCustomerID 11 (James Carter) is an exact copy of CustomerID 1 \u2014 same name, email, phone, DOB.\n\n---\n\n## Solution Query\n\n```sql\n-- Step 1: Find duplicate emails (the natural business key)\nSELECT\n    Email,\n    COUNT(*) AS Occurrences,\n    MIN(CustomerID) AS FirstID,\n    MAX(CustomerID) AS LastID\nFROM Customers\nWHERE Email IS NOT NULL\nGROUP BY Email\nHAVING COUNT(*) > 1;\n\n-- Step 2: Show the full duplicate rows side-by-side using ROW_NUMBER\nWITH Ranked AS (\n    SELECT *,\n        ROW_NUMBER() OVER (\n            PARTITION BY Email\n            ORDER BY CustomerID ASC\n        ) AS RowRank\n    FROM Customers\n    WHERE Email IS NOT NULL\n)\nSELECT\n    CustomerID,\n    FirstName,\n    LastName,\n    Email,\n    CreatedDate,\n    RowRank,\n    CASE WHEN RowRank = 1 THEN 'KEEP' ELSE 'REVIEW/DELETE' END AS Action\nFROM Ranked\nWHERE Email IN (\n    SELECT Email FROM Customers\n    WHERE Email IS NOT NULL\n    GROUP BY Email HAVING COUNT(*) > 1\n)\nORDER BY Email, RowRank;\n\n-- Step 3: Count exact full-row duplicates (every column matches)\nSELECT\n    FirstName, LastName, Email, Phone, DOB, Country,\n    COUNT(*) AS DuplicateCount\nFROM Customers\nGROUP BY FirstName, LastName, Email, Phone, DOB, Country\nHAVING COUNT(*) > 1;\n```\n\n---\n\n## Expected Output\n\n**Step 1:**\n| Email | Occurrences | FirstID | LastID |\n|---|---|---|---|\n| james.carter@email.com | 2 | 1 | 11 |\n\n**Step 2:**\n| CustomerID | FullName | Email | RowRank | Action |\n|---|---|---|---|---|\n| 1 | James Carter | james.carter@email.com | 1 | KEEP |\n| 11 | James Carter | james.carter@email.com | 2 | REVIEW/DELETE |\n\n---\n\n## Explanation\n\n### The classic duplicate pattern: GROUP BY with HAVING\n`GROUP BY ... HAVING COUNT(*) > 1` groups rows by the natural business key (Email here)\nand filters to only the groups where more than one row shares that key. This is the\nfastest and most readable duplicate detection pattern in SQL \u2014 you will use it in\nalmost every data quality engagement.\n\n### Ranking duplicates to decide which one to keep\n`ROW_NUMBER() OVER (PARTITION BY Email ORDER BY CustomerID)` assigns a sequential\nnumber within each group of duplicates, restarting at 1 for each unique Email.\nThe row ranked 1 (the lowest CustomerID, meaning earliest created) becomes the\ndesignated \"master\" record. All rows with RowRank > 1 are candidates for deletion\nor archiving. This numbered ranking is the standard preparation step before running\na deduplication DELETE or MERGE operation.\n\n### Full-row duplicate vs key-only duplicate\nStep 3 checks for rows where every business column is identical, even if the\nsurrogate key (CustomerID) is different. This catches re-loaded records where the\nETL assigned a new ID but the actual content was already in the table \u2014 a common\nbug when source files are re-processed without a deduplication guard.\n\n---\n\n## Interview Angle\n*\"How would you remove duplicates while keeping one row?\"*\n```sql\nDELETE FROM Customers\nWHERE CustomerID NOT IN (\n    SELECT MIN(CustomerID)\n    FROM Customers\n    GROUP BY Email\n);\n```\nAlways SELECT before DELETE to verify what will be removed.\n\n---\n\n## Severity: **Critical**\nDuplicate customer records cause double-billing, inflated marketing lists, and broken\none-to-many relationships with Orders.\n",
  "M02_Uniqueness/M02-E2_fuzzy_near_duplicates": "# M02 \u2014 Uniqueness | Example 2: Near-Duplicate / Fuzzy Match Detection\n\n## The Question\n> Your deduplication run from Example 1 caught exact duplicates. But the data quality\n> manager flags another issue:\n> **\"We still see what look like the same person with slightly different names \u2014\n> 'Sara Thompson' and 'Sarah Thompson' both share the same email address.\n> How do you detect near-duplicates that exact matching misses?\"**\n\n---\n\n## Table Reference\n**Employees** \u2014 `schemas/04_Employees.csv`\n\nE001 = \"Sarah Thompson\" and E015 = \"Sara Thompson\" \u2014 both share sarah.thompson@corp.com.\n\n---\n\n## Solution Query\n\n```sql\n-- Method A: Shared business key (same email, different name or ID)\nSELECT\n    A.EmployeeID   AS ID_A,\n    A.FullName     AS Name_A,\n    B.EmployeeID   AS ID_B,\n    B.FullName     AS Name_B,\n    A.Email,\n    A.Department,\n    'Shared Email \u2014 possible near-duplicate' AS Flag\nFROM Employees A\nJOIN Employees B\n    ON  A.Email       = B.Email\n    AND A.EmployeeID <> B.EmployeeID\n    AND A.EmployeeID  < B.EmployeeID   -- avoids showing (A,B) and (B,A)\nWHERE A.Email IS NOT NULL;\n\n-- Method B: SOUNDEX \u2014 same phonetic sound of name, same department\nSELECT\n    A.EmployeeID   AS ID_A,\n    A.FullName     AS Name_A,\n    B.EmployeeID   AS ID_B,\n    B.FullName     AS Name_B,\n    A.Department,\n    SOUNDEX(A.FullName) AS SoundexCode,\n    'Phonetic match in same department' AS Flag\nFROM Employees A\nJOIN Employees B\n    ON  SOUNDEX(A.FullName) = SOUNDEX(B.FullName)\n    AND A.Department        = B.Department\n    AND A.EmployeeID       <> B.EmployeeID\n    AND A.EmployeeID        < B.EmployeeID;\n\n-- Method C: DIFFERENCE() \u2014 similarity score 0-4 (4 = very similar)\nSELECT\n    A.EmployeeID   AS ID_A,\n    A.FullName     AS Name_A,\n    B.EmployeeID   AS ID_B,\n    B.FullName     AS Name_B,\n    DIFFERENCE(A.FullName, B.FullName) AS SimilarityScore\nFROM Employees A\nJOIN Employees B\n    ON  A.EmployeeID       <> B.EmployeeID\n    AND A.EmployeeID        < B.EmployeeID\nWHERE DIFFERENCE(A.FullName, B.FullName) >= 3;  -- 3 or 4 = high similarity\n```\n\n---\n\n## Expected Output\n\n**Method A (shared email):**\n| ID_A | Name_A | ID_B | Name_B | Email | Flag |\n|---|---|---|---|---|---|\n| E001 | Sarah Thompson | E015 | Sara Thompson | sarah.thompson@corp.com | Shared Email \u2014 possible near-duplicate |\n\n**Method B (SOUNDEX):**\n| ID_A | Name_A | ID_B | Name_B | Department | SoundexCode |\n|---|---|---|---|---|---|\n| E001 | Sarah Thompson | E015 | Sara Thompson | Sales | S636 |\n\n---\n\n## Explanation\n\n### How SOUNDEX converts names into phonetic codes\nSOUNDEX converts a string into a 4-character code that represents how it sounds,\nnot how it is spelled. The first character is the first letter; the remaining\nthree are digits encoding consonant sounds. \"Sarah\" and \"Sara\" produce the same\ncode because they sound identical when spoken. This makes SOUNDEX powerful for\ncatching name typos, nicknames, and transcription errors that exact matching misses.\n\n### Using DIFFERENCE() as a similarity score\nDIFFERENCE() compares two SOUNDEX codes and returns a score from 0 (no similarity)\nto 4 (near-identical sound). A score of 3 or 4 is a strong near-duplicate signal.\nIt works best on single words \u2014 for full names, apply it to the full FullName string\nor split first and last names and score them separately for more precision.\n\n### These are candidate flags, not confirmed duplicates\nBoth SOUNDEX and DIFFERENCE are heuristics \u2014 they surface rows that look similar,\nbut a human or a second confirming rule must decide whether two records are truly\nthe same person. The standard pattern is: (1) flag candidates with SOUNDEX, then\n(2) narrow by a second attribute like hire date, department, or salary before\nmarking as confirmed duplicates.\n\n---\n\n## Interview Angle\n*\"How do you detect duplicates where names are spelled slightly differently?\"*\nUse SOUNDEX or DIFFERENCE for phonetic matching. For more advanced fuzzy matching\n(e.g., Levenshtein distance), use Python with `fuzzywuzzy` or SQL CLR functions.\nIn Azure, Azure Cognitive Search and Purview have built-in entity matching.\n\n---\n\n## Severity: **High**\nNear-duplicates inflate headcounts, cause duplicate payroll runs, and break\nrole-based access controls if the same person has two active accounts.\n",
  "M03_Referential_Integrity/M03-E1_orphan_records": "# M03 \u2014 Referential Integrity | Example 1: Orphan Records (FK Violations)\n\n## The Question\n> The Orders table references customers and products that must exist in their\n> parent tables. After a batch load, your lead asks:\n> **\"Are there any orders that reference a customer or product that doesn't exist\n> in our system? These orphan records will break the reporting joins.\"**\n> Find all orphaned orders and report which FK column is broken.\n\n---\n\n## Table Reference\n**Orders** \u2014 `schemas/02_Orders.csv`\n**Customers** \u2014 `schemas/01_Customers.csv`\n**Products** \u2014 `schemas/03_Products.csv`\n\nKnown issue: OrderID 1004 has CustomerID = 99, which does not exist in Customers.\n\n---\n\n## Solution Query\n\n```sql\n-- Method A: LEFT JOIN approach \u2014 gold standard for FK validation\nSELECT\n    o.OrderID,\n    o.CustomerID,\n    o.ProductID,\n    o.OrderDate,\n    o.TotalAmount,\n    CASE\n        WHEN c.CustomerID IS NULL AND p.ProductID IS NULL THEN 'Both FK missing'\n        WHEN c.CustomerID IS NULL                         THEN 'CustomerID not found'\n        WHEN p.ProductID  IS NULL                         THEN 'ProductID not found'\n    END AS Violation\nFROM Orders o\nLEFT JOIN Customers c ON o.CustomerID = c.CustomerID\nLEFT JOIN Products  p ON o.ProductID  = p.ProductID\nWHERE c.CustomerID IS NULL\n   OR p.ProductID  IS NULL;\n\n-- Method B: NOT EXISTS \u2014 readable alternative, sometimes faster\nSELECT\n    OrderID,\n    CustomerID,\n    'CustomerID orphan' AS ViolationType\nFROM Orders o\nWHERE NOT EXISTS (\n    SELECT 1 FROM Customers c WHERE c.CustomerID = o.CustomerID\n)\nUNION ALL\nSELECT\n    OrderID,\n    ProductID,\n    'ProductID orphan'\nFROM Orders o\nWHERE NOT EXISTS (\n    SELECT 1 FROM Products p WHERE p.ProductID = o.ProductID\n);\n\n-- Method C: Summary count for a data quality dashboard\nSELECT\n    'Orders \u2192 Customers (CustomerID)' AS FK_Check,\n    COUNT(*) AS OrphanCount\nFROM Orders o\nWHERE NOT EXISTS (SELECT 1 FROM Customers c WHERE c.CustomerID = o.CustomerID)\nUNION ALL\nSELECT\n    'Orders \u2192 Products (ProductID)',\n    COUNT(*)\nFROM Orders o\nWHERE NOT EXISTS (SELECT 1 FROM Products p WHERE p.ProductID = o.ProductID);\n```\n\n---\n\n## Expected Output\n\n**Method A:**\n| OrderID | CustomerID | ProductID | ViolationType |\n|---|---|---|---|\n| 1004 | 99 | P003 | CustomerID not found |\n\n---\n\n## Explanation\n\n### The LEFT JOIN trick \u2014 turning a join into a gap finder\nA LEFT JOIN returns every row from the left table (Orders) plus matched columns from\nthe right table (Customers). Where no match exists, the right-side columns come back\nas NULL. By adding `WHERE c.CustomerID IS NULL` after the join, you filter down to\nonly those unmatched rows \u2014 which are precisely the FK violations. This is one of\nthe most important patterns in data validation SQL.\n\n### NOT EXISTS \u2014 the readable alternative\n`NOT EXISTS (SELECT 1 FROM Customers WHERE ...)` reads almost like plain English:\n\"give me orders where no matching customer exists.\" Both LEFT JOIN and NOT EXISTS\nproduce the same rows. The query optimiser often generates identical execution plans\nfor both, but NOT EXISTS can be faster when the FK table is large and well-indexed,\nbecause it short-circuits as soon as it finds one match.\n\n### Method C \u2014 the dashboard health number\nMethod C collapses the detail into a single count per FK relationship. Zero = clean load.\nAny count above zero triggers an investigation. This summary format is what you put on\na data quality monitoring dashboard so operations teams can see ETL health at a glance\nwithout reading individual rows.\n\n---\n\n## Interview Angle\n*\"What is the difference between LEFT JOIN IS NULL and NOT EXISTS for FK checks?\"*\nBoth detect the same rows. NOT EXISTS short-circuits as soon as it finds one match\n(efficient when the FK table is large and indexed). LEFT JOIN IS NULL materialises\nthe full join first. In practice, the query optimiser often produces the same plan.\n\n---\n\n## Severity: **Critical**\nOrphan records cause INNER JOIN reports to silently drop rows, leading to understated\nrevenue figures and broken drill-downs in Power BI.\n",
  "M03_Referential_Integrity/M03-E2_self_referencing_hierarchy": "# M03 \u2014 Referential Integrity | Example 2: Self-Referencing Hierarchy Validation\n\n## The Question\n> The `Employees` table uses a self-referencing foreign key: `ManagerID` points back\n> to `EmployeeID` in the same table to represent the org hierarchy.\n> Your HR team asks:\n> **\"Validate the manager hierarchy \u2014 find anyone whose ManagerID points to\n> an employee that doesn't exist, anyone reporting to themselves,\n> and any circular loops where A reports to B and B reports to A.\"**\n\n---\n\n## Table Reference\n**Employees** \u2014 `schemas/04_Employees.csv`\n\nKnown issue: E017 has ManagerID = E099, which does not exist.\nTop-level managers (E010\u2013E013) have NULL ManagerID \u2014 this is valid.\n\n---\n\n## Solution Query\n\n```sql\n-- Check 1: ManagerID references a non-existent EmployeeID\nSELECT\n    e.EmployeeID,\n    e.FullName,\n    e.ManagerID,\n    'Manager does not exist' AS Issue\nFROM Employees e\nWHERE e.ManagerID IS NOT NULL\n  AND NOT EXISTS (\n      SELECT 1 FROM Employees m WHERE m.EmployeeID = e.ManagerID\n  );\n\n-- Check 2: Employee reporting to themselves (self-loop)\nSELECT\n    EmployeeID,\n    FullName,\n    ManagerID,\n    'Self-referencing \u2014 reports to themselves' AS Issue\nFROM Employees\nWHERE ManagerID = EmployeeID;\n\n-- Check 3: Circular loop \u2014 A is manager of B, B is manager of A\nSELECT\n    A.EmployeeID  AS EmpA,\n    A.FullName    AS NameA,\n    B.EmployeeID  AS EmpB,\n    B.FullName    AS NameB,\n    'Circular reporting loop' AS Issue\nFROM Employees A\nJOIN Employees B\n    ON  A.ManagerID = B.EmployeeID\n    AND B.ManagerID = A.EmployeeID;\n\n-- Check 4: Full hierarchy validation using Recursive CTE\n-- Shows every employee with their chain up to the root\nWITH OrgHierarchy AS (\n    -- Anchor: top-level managers (no manager)\n    SELECT\n        EmployeeID,\n        FullName,\n        ManagerID,\n        0 AS Level,\n        CAST(EmployeeID AS VARCHAR(500)) AS Path\n    FROM Employees\n    WHERE ManagerID IS NULL\n\n    UNION ALL\n\n    -- Recursive: employees with a manager\n    SELECT\n        e.EmployeeID,\n        e.FullName,\n        e.ManagerID,\n        oh.Level + 1,\n        CAST(oh.Path + ' > ' + e.EmployeeID AS VARCHAR(500))\n    FROM Employees e\n    JOIN OrgHierarchy oh ON e.ManagerID = oh.EmployeeID\n)\nSELECT\n    EmployeeID,\n    FullName,\n    ManagerID,\n    Level,\n    Path\nFROM OrgHierarchy\nORDER BY Path;\n```\n\n---\n\n## Expected Output\n\n**Check 1 (non-existent manager):**\n| EmployeeID | FullName | ManagerID | Issue |\n|---|---|---|---|\n| E017 | John Smith | E099 | Manager does not exist |\n\n**Check 4 (hierarchy):**\n| EmployeeID | FullName | Level | Path |\n|---|---|---|---|\n| E010 | Robert Blake | 0 | E010 |\n| E001 | Sarah Thompson | 1 | E010 > E001 |\n| E005 | Nadia Costa | 1 | E010 > E005 |\n| E011 | Claire Fontaine | 0 | E011 |\n| E002 | Rajiv Menon | 1 | E011 > E002 |\n\n---\n\n## Explanation\n\n### Validating a self-referencing FK with a table alias\nCheck 1 runs a NOT EXISTS against the same table using two different aliases (`e` for\nthe employee being checked, `m` for the manager being looked up). This is standard\nfor any self-referencing hierarchy \u2014 org charts, product category trees, geographic\nregion rollups. The logic is identical to orphan checking (M03-E1) except both sides\nof the join point to the same table.\n\n### How a Recursive CTE walks a hierarchy\nThe Recursive CTE in Check 4 has two mandatory parts. The **anchor** selects the root\nlevel \u2014 employees with no manager (`ManagerID IS NULL`). The **recursive member** joins\neach new level of employees back to the previous CTE result using `ManagerID = EmployeeID`.\nSQL Server keeps looping until no new rows are added. The `Path` column builds a trail\nlike `E010 > E001` so you can read the full chain from any employee back to the top.\nSQL Server's default recursion limit is 100 levels \u2014 raise it with `OPTION (MAXRECURSION n)`\nif your hierarchy is deeper.\n\n### Always check for circular loops before recursing\nA circular reference \u2014 where A manages B and B manages A \u2014 causes the recursive CTE\nto loop forever and throw an error. Check 3 catches circles first with a simple self-join.\nRun Check 1 (ghost managers) and Check 3 (circles) before trusting the output of Check 4.\n\n---\n\n## Interview Angle\n*\"How do you validate a hierarchical dataset like an org chart?\"*\nThree-step process: (1) check FK exists, (2) check for self-loops, (3) check for circles.\nThen use a Recursive CTE to traverse the valid hierarchy and verify depth/path makes sense.\n\n---\n\n## Severity: **High**\nBroken hierarchies corrupt roll-up reporting (e.g., regional revenue aggregation),\naccess control inheritance, and org-chart visualisations.\n",
  "M04_Format_Pattern/M04-E1_email_format": "# M04 \u2014 Format & Pattern | Example 1: Email Address Validation\n\n## The Question\n> The marketing team is about to send a campaign to the entire Customers table.\n> Before the send, compliance asks:\n> **\"Validate that every email address in our system is properly formatted.\n> Give us a breakdown of the types of format issues found, not just a yes/no flag.\"**\n\n---\n\n## Table Reference\n**Customers** \u2014 `schemas/01_Customers.csv`\n\nKnown issues: Row 7 has \"tom.brooks\" (no @domain). Row 6 has \"ANJALI.SHARMA@EMAIL.COM\"\n(all caps \u2014 valid format but inconsistent casing). Row 16 has \"N/A\".\n\n---\n\n## Solution Query\n\n```sql\n-- Classify every email by its format issue\nSELECT\n    CustomerID,\n    FirstName + ' ' + LastName AS CustomerName,\n    Email,\n    CASE\n        WHEN Email IS NULL\n            THEN 'NULL \u2014 missing'\n        WHEN LTRIM(RTRIM(Email)) = ''\n            THEN 'Blank string'\n        WHEN UPPER(Email) IN ('N/A','NA','NONE','UNKNOWN')\n            THEN 'Placeholder value'\n        WHEN Email NOT LIKE '%@%'\n            THEN 'No @ symbol'\n        WHEN Email LIKE '@%'\n            THEN 'Starts with @ \u2014 no local part'\n        WHEN Email LIKE '%@'\n            THEN 'Ends with @ \u2014 no domain'\n        WHEN Email NOT LIKE '%@%.%'\n            THEN 'No dot in domain'\n        WHEN Email LIKE '% %'\n            THEN 'Contains a space'\n        WHEN LEN(Email) - LEN(REPLACE(Email,'@','')) > 1\n            THEN 'More than one @ symbol'\n        WHEN Email <> LOWER(Email)\n            THEN 'Mixed/upper case \u2014 normalise before send'\n        ELSE 'Valid format'\n    END AS EmailFormatStatus\nFROM Customers\nORDER BY EmailFormatStatus, CustomerID;\n\n-- Summary count by status\nSELECT\n    CASE\n        WHEN Email IS NULL                          THEN 'NULL'\n        WHEN LTRIM(RTRIM(Email)) = ''               THEN 'Blank'\n        WHEN UPPER(Email) IN ('N/A','NA','NONE')    THEN 'Placeholder'\n        WHEN Email NOT LIKE '%@%.%'                 THEN 'Invalid format'\n        WHEN Email <> LOWER(Email)                  THEN 'Casing issue'\n        ELSE 'Valid'\n    END AS Status,\n    COUNT(*) AS RecordCount\nFROM Customers\nGROUP BY\n    CASE\n        WHEN Email IS NULL                          THEN 'NULL'\n        WHEN LTRIM(RTRIM(Email)) = ''               THEN 'Blank'\n        WHEN UPPER(Email) IN ('N/A','NA','NONE')    THEN 'Placeholder'\n        WHEN Email NOT LIKE '%@%.%'                 THEN 'Invalid format'\n        WHEN Email <> LOWER(Email)                  THEN 'Casing issue'\n        ELSE 'Valid'\n    END\nORDER BY RecordCount DESC;\n```\n\n---\n\n## Expected Output\n\n| CustomerID | CustomerName | Email | EmailFormatStatus |\n|---|---|---|---|\n| 3 | Mohammed Al-Rashid | NULL | NULL \u2014 missing |\n| 7 | Tom Brooks | tom.brooks | No dot in domain |\n| 16 | N/A N/A | N/A | Placeholder value |\n| 6 | Anjali Sharma | ANJALI.SHARMA@EMAIL.COM | Mixed/upper case \u2014 normalise before send |\n| 12 | Ravi Kumar | NULL | NULL \u2014 missing |\n\n---\n\n## Explanation\n\n### How LIKE pattern matching works in T-SQL\nSQL `LIKE` patterns use `%` to mean \"any sequence of zero or more characters\" and\n`_` to mean \"exactly one character.\" So `'%@%.%'` means: something, then @, then\nsomething, then a dot, then something \u2014 the minimum structure of a valid email address.\nThese patterns are not as powerful as regular expressions but cover most common\nformat rules without needing CLR extensions.\n\n### Why CASE WHEN order matters\nThe CASE WHEN chain evaluates top-down and stops at the first matching condition.\nThis means you must order from broadest to most specific: check for NULL first\n(because any comparison involving NULL returns UNKNOWN, not TRUE, so putting NULL\nlast would cause it to fall through every condition silently), then blanks,\nthen structural rules. Getting the order wrong causes conditions to never fire.\n\n### Counting @ symbols without a loop\nThe expression `LEN(Email) - LEN(REPLACE(Email, '@', ''))` counts the number of @\nsymbols in a string. REPLACE removes every @, then the length difference tells you\nhow many were removed. If the difference is greater than 1, there are multiple @\nsigns \u2014 an immediate format failure.\n\n### Casing normalisation \u2014 valid but inconsistent\nAn email like `ANJALI@EMAIL.COM` is technically RFC-valid, but storing mixed-case\nemails causes lookup mismatches in systems that compare case-sensitively. The best\npractice is to store all emails as `LOWER(Email)` at the point of data entry or\nduring the ETL load, before they reach the main table.\n\n---\n\n## Interview Angle\n*\"Can you fully validate an email address in SQL with LIKE?\"*\nNo. LIKE can catch obvious structural failures (missing @, no domain) but cannot\nvalidate that the domain actually exists or that the mailbox accepts mail.\nFor full validation you need a regex (available in SQL Server via CLR or\nCHECK constraint with LIKE) or an external email verification API.\n\n---\n\n## Severity: **High**\nInvalid emails cause bounce rates that can get your sending domain blacklisted.\n",
  "M05_Range_Boundary/M05-E1_negative_and_zero_values": "# M05 \u2014 Range & Boundary | Example 1: Negative, Zero, and Out-of-Range Values\n\n## The Question\n> The finance team runs a monthly P&L reconciliation and finds the numbers don't add up.\n> You suspect data entry errors in the Orders and Products tables.\n> **\"Find all records where numeric values violate basic business rules:\n> negative quantities, zero prices, negative costs, and selling prices\n> below cost price. Explain what each violation means commercially.\"**\n\n---\n\n## Table References\n**Orders** \u2014 `schemas/02_Orders.csv`\n**Products** \u2014 `schemas/03_Products.csv`\n\nKnown issues: OrderID 1007 has Quantity = 0. OrderID 1020 has negative UnitPrice.\nP009 sells below cost. P013 has negative StockQty. P015 has SellingPrice = 0.\n\n---\n\n## Solution Query\n\n```sql\n-- Orders: numeric boundary violations\nSELECT\n    OrderID,\n    CustomerID,\n    Quantity,\n    UnitPrice,\n    TotalAmount,\n    DiscountPct,\n    CASE\n        WHEN Quantity     <= 0  THEN 'Quantity must be > 0'\n        WHEN UnitPrice    <= 0  THEN 'Unit price must be > 0'\n        WHEN TotalAmount  <  0  THEN 'Total amount cannot be negative'\n        WHEN DiscountPct  <  0  THEN 'Discount cannot be negative'\n        WHEN DiscountPct  > 100 THEN 'Discount cannot exceed 100%'\n        ELSE 'OK'\n    END AS Violation\nFROM Orders\nWHERE Quantity     <= 0\n   OR UnitPrice    <= 0\n   OR TotalAmount  <  0\n   OR DiscountPct NOT BETWEEN 0 AND 100;\n\n-- Products: cost vs price and stock violations\nSELECT\n    ProductID,\n    ProductName,\n    UnitCost,\n    SellingPrice,\n    StockQty,\n    CASE\n        WHEN SellingPrice <= 0              THEN 'Selling price is zero or negative'\n        WHEN SellingPrice < UnitCost        THEN 'Selling BELOW cost \u2014 negative margin'\n        WHEN SellingPrice = UnitCost        THEN 'Zero margin \u2014 selling at cost price'\n        WHEN StockQty     < 0              THEN 'Negative stock quantity \u2014 impossible'\n        ELSE 'OK'\n    END AS Violation,\n    CASE\n        WHEN UnitCost > 0 AND SellingPrice > UnitCost\n            THEN CAST(((SellingPrice - UnitCost) / SellingPrice) * 100 AS DECIMAL(5,2))\n        ELSE NULL\n    END AS GrossMarginPct\nFROM Products\nWHERE SellingPrice <= 0\n   OR SellingPrice <= UnitCost\n   OR StockQty     < 0;\n\n-- Employees: negative salary\nSELECT\n    EmployeeID,\n    FullName,\n    Salary,\n    'Salary cannot be negative or zero' AS Violation\nFROM Employees\nWHERE Salary <= 0;\n```\n\n---\n\n## Expected Output\n\n**Orders violations:**\n| OrderID | Quantity | UnitPrice | TotalAmount | Violation |\n|---|---|---|---|---|\n| 1007 | 0 | 49.99 | 0.00 | Quantity must be > 0 |\n| 1020 | 1 | -50.00 | -50.00 | Unit price must be > 0 |\n\n**Products violations:**\n| ProductID | ProductName | UnitCost | SellingPrice | StockQty | Violation | GrossMarginPct |\n|---|---|---|---|---|---|---|\n| P009 | Headset Pro | 110.00 | 90.00 | 35 | Selling BELOW cost \u2014 negative margin | NULL |\n| P010 | Office Chair | 180.00 | 180.00 | 12 | Zero margin \u2014 selling at cost price | NULL |\n| P013 | Mousepad XL | 8.00 | 24.99 | -5 | Negative stock quantity \u2014 impossible | NULL |\n| P015 | USB Drive 256GB | 12.00 | 0.00 | 200 | Selling price is zero or negative | NULL |\n\n---\n\n## Explanation\n\n### BETWEEN is inclusive \u2014 boundary values count\n`BETWEEN 0 AND 100` is equivalent to `>= 0 AND <= 100`. Both endpoints are included.\nFor a discount percentage, 0% (no discount applied) and 100% (item is free) sit at\nthe boundaries \u2014 whether these are valid depends on the business rule. Always clarify\nwith stakeholders whether the fences are strict (`> 0`, `< 100`) or relaxed\n(`>= 0`, `<= 100`) before writing the check.\n\n### Gross margin \u2014 the commercial health signal\nThe gross margin formula `(SellingPrice - UnitCost) / SellingPrice \u00d7 100` expresses\nprofit as a percentage of the selling price. A healthy retail margin is typically\nabove 30%. Anything at 0% means the product is sold at cost with no profit. Anything\nnegative means the business loses money on every unit sold. Finding these during\na data validation check is a critical commercial alert \u2014 not just a data quality flag.\n\n### Negative stock \u2014 two possible causes\nA negative StockQty value is impossible in the physical world and always indicates\na system problem. The two most common causes are: (1) a return or adjustment was\nprocessed against the wrong product, decrementing stock that was never held; or\n(2) a timing issue in concurrent warehouse writes where the decrement ran before\nthe receipt was recorded. Either way, the validation query surfaces it for investigation.\n\n---\n\n## Interview Angle\n*\"What is the BETWEEN operator in SQL and what are its boundaries?\"*\nBETWEEN is inclusive: `WHERE Salary BETWEEN 50000 AND 80000` returns rows where\nSalary is exactly 50000, exactly 80000, and everything in between. For exclusive\nupper bounds use `>= X AND < Y`.\n\n---\n\n## Severity: **Critical**\nNegative prices corrupt revenue reports. Zero-quantity orders inflate order counts.\nNegative stock breaks reorder logic.\n",
  "M05_Range_Boundary/M05-E2_date_range_violations": "# M05 \u2014 Range & Boundary | Example 2: Date Range and Sequence Violations\n\n## The Question\n> The operations team is reviewing shipment data and flagging anomalies.\n> **\"Find all orders where the ship date is before the order date,\n> orders placed with an unreasonably old date (more than 1 year ago when status is Pending),\n> and customers who were registered as minors (under 18) based on their date of birth.\"**\n\n---\n\n## Table References\n**Orders** \u2014 `schemas/02_Orders.csv`\n**Customers** \u2014 `schemas/01_Customers.csv`\n\nKnown issues: OrderID 1006 ShipDate < OrderDate. OrderID 1019 has a 2023 OrderDate.\nCustomer row 8 (DOB 2005) and row 15 (DOB 2010) may be minors.\n\n---\n\n## Solution Query\n\n```sql\n-- Check 1: Ship date before order date (impossible sequence)\nSELECT\n    OrderID,\n    CustomerID,\n    OrderDate,\n    ShipDate,\n    DATEDIFF(DAY, OrderDate, ShipDate) AS DaysDiff,\n    'ShipDate is BEFORE OrderDate \u2014 impossible' AS Violation\nFROM Orders\nWHERE ShipDate IS NOT NULL\n  AND ShipDate < OrderDate;\n\n-- Check 2: Stale pending orders \u2014 ordered over 1 year ago but still Pending\nSELECT\n    OrderID,\n    CustomerID,\n    OrderDate,\n    Status,\n    DATEDIFF(DAY, OrderDate, GETDATE()) AS DaysOld,\n    'Order is over 365 days old and still ' + Status AS Violation\nFROM Orders\nWHERE Status IN ('Pending','Processing')\n  AND OrderDate < DATEADD(YEAR, -1, GETDATE());\n\n-- Check 3: Future-dated orders (order date in the future \u2014 data entry error)\nSELECT\n    OrderID,\n    OrderDate,\n    'Order date is in the future' AS Violation\nFROM Orders\nWHERE OrderDate > GETDATE();\n\n-- Check 4: Minor customers (under 18 based on DOB)\nSELECT\n    CustomerID,\n    FirstName + ' ' + LastName AS CustomerName,\n    DOB,\n    DATEDIFF(YEAR, DOB, GETDATE())\n        - CASE\n            WHEN MONTH(DOB) > MONTH(GETDATE())\n              OR (MONTH(DOB) = MONTH(GETDATE()) AND DAY(DOB) > DAY(GETDATE()))\n            THEN 1 ELSE 0\n          END AS AgeYears,\n    'Customer is under 18 \u2014 age-restricted services blocked' AS Flag\nFROM Customers\nWHERE DOB IS NOT NULL\n  AND DATEDIFF(YEAR, DOB, GETDATE())\n        - CASE\n            WHEN MONTH(DOB) > MONTH(GETDATE())\n              OR (MONTH(DOB) = MONTH(GETDATE()) AND DAY(DOB) > DAY(GETDATE()))\n            THEN 1 ELSE 0\n          END < 18;\n\n-- Check 5: DOB in the future (impossible)\nSELECT\n    CustomerID,\n    FirstName + ' ' + LastName AS CustomerName,\n    DOB,\n    'Date of Birth is in the future \u2014 impossible' AS Violation\nFROM Customers\nWHERE DOB > GETDATE();\n```\n\n---\n\n## Expected Output\n\n**Check 1 (ship before order):**\n| OrderID | OrderDate | ShipDate | DaysDiff | Violation |\n|---|---|---|---|---|\n| 1006 | 2024-01-22 | 2024-01-19 | -3 | ShipDate is BEFORE OrderDate \u2014 impossible |\n\n**Check 4 (minors \u2014 approximate, depends on run date):**\n| CustomerID | CustomerName | DOB | AgeYears | Flag |\n|---|---|---|---|---|\n| 8 | Fatima Hassan | 2005-06-14 | ~19 or 20 | (may clear 18 by run date) |\n| 15 | Lily Zhang | 2010-03-07 | ~15 | Customer is under 18 |\n\n---\n\n## Explanation\n\n### DATEDIFF \u2014 measuring the gap between two dates\n`DATEDIFF(DAY, start, end)` counts the number of day boundaries crossed between the\nstart and end dates. A positive result means end is after start (normal). A negative\nresult means end is before start \u2014 which is impossible for a shipment and flags the\nrow as a data entry error. The `DAY` unit can be swapped for `MONTH`, `YEAR`, `HOUR`\ndepending on what granularity the check needs.\n\n### Getting someone's true age \u2014 why DATEDIFF(YEAR) is not enough\n`DATEDIFF(YEAR, DOB, GETDATE())` counts year boundaries crossed, not actual completed\nyears. A person born on 31 December 1990 would show as 1 year older on 1 January 1991,\neven though they are only a few hours old. The `CASE` block in Check 4 corrects this:\nit subtracts 1 if the current date has not yet passed the person's birthday in the\ncurrent year, giving the true number of completed years of age.\n\n### DATEADD \u2014 stepping forward and backward from a date\n`DATEADD(YEAR, -1, GETDATE())` produces the date exactly one year before today.\nUse positive values to move forward, negative values to move backward. Common patterns:\n`DATEADD(DAY, -7, GETDATE())` for the last 7 days, `DATEADD(MONTH, -1, ...)` for\nthe previous month start, `DATEADD(YEAR, -18, GETDATE())` for the 18-years-ago\nthreshold in age checks.\n\n### GETDATE() vs CAST(GETDATE() AS DATE)\n`GETDATE()` returns both a date and a time component (e.g., `2024-03-20 14:32:17`).\nWhen comparing against a DATE-only column, the time part can cause unexpected\nmismatches. Wrapping it in `CAST(GETDATE() AS DATE)` strips the time and gives a\nclean date comparison.\n\n---\n\n## Interview Angle\n*\"How do you calculate someone's exact age in SQL?\"*\nUse the DATEDIFF(YEAR) + birthday-not-yet-reached adjustment shown in Check 4.\nA simpler but slightly less accurate shortcut: `DATEDIFF(DAY, DOB, GETDATE()) / 365`.\n\n---\n\n## Severity: **Critical** (sequence) / **High** (age verification)\nReversed dates invalidate SLA calculations. Minors accessing age-restricted products\nis a legal/compliance risk.\n",
  "M06_Consistency/M06-E1_calculated_field_mismatch": "# M06 \u2014 Consistency | Example 1: Calculated Field Mismatch\n\n## The Question\n> The finance controller notices the Orders table stores both line-item components\n> (Quantity \u00d7 UnitPrice \u00d7 Discount) AND a pre-computed TotalAmount.\n> She asks: **\"Are the stored TotalAmount values actually correct?\n> Find every order where TotalAmount does not match what we would calculate\n> from its own components, and tell us the size of the discrepancy.\"**\n\n---\n\n## Table Reference\n**Orders** \u2014 `schemas/02_Orders.csv`\n\nBusiness rule: `TotalAmount = Quantity \u00d7 UnitPrice \u00d7 (1 - DiscountPct / 100)`\n\nKnown issue: OrderID 1005 has TotalAmount = 350.00 but the calculation gives 359.98.\n\n---\n\n## Solution Query\n\n```sql\n-- Identify mismatches between stored and calculated TotalAmount\nSELECT\n    OrderID,\n    CustomerID,\n    Quantity,\n    UnitPrice,\n    DiscountPct,\n    TotalAmount                                 AS StoredTotal,\n    CAST(\n        Quantity * UnitPrice * (1 - DiscountPct / 100.0)\n    AS DECIMAL(10,2))                           AS CalculatedTotal,\n    CAST(\n        TotalAmount - (Quantity * UnitPrice * (1 - DiscountPct / 100.0))\n    AS DECIMAL(10,2))                           AS Discrepancy,\n    CASE\n        WHEN ABS(TotalAmount - (Quantity * UnitPrice * (1 - DiscountPct / 100.0))) < 0.01\n            THEN 'OK (within rounding)'\n        WHEN TotalAmount > Quantity * UnitPrice * (1 - DiscountPct / 100.0)\n            THEN 'Stored total is OVER-STATED'\n        ELSE 'Stored total is UNDER-STATED'\n    END AS MismatchType\nFROM Orders\nWHERE ABS(\n    TotalAmount - (Quantity * UnitPrice * (1 - DiscountPct / 100.0))\n) > 0.01   -- 1 cent tolerance for floating point rounding\nORDER BY ABS(TotalAmount - (Quantity * UnitPrice * (1 - DiscountPct / 100.0))) DESC;\n\n-- Summary: total financial exposure from mismatches\nSELECT\n    COUNT(*) AS MismatchedOrders,\n    SUM(TotalAmount) AS StoredRevenue,\n    SUM(Quantity * UnitPrice * (1 - DiscountPct / 100.0)) AS CalculatedRevenue,\n    SUM(TotalAmount) - SUM(Quantity * UnitPrice * (1 - DiscountPct / 100.0))\n        AS TotalExposure\nFROM Orders\nWHERE ABS(\n    TotalAmount - (Quantity * UnitPrice * (1 - DiscountPct / 100.0))\n) > 0.01;\n```\n\n---\n\n## Expected Output\n\n**Row detail:**\n| OrderID | StoredTotal | CalculatedTotal | Discrepancy | MismatchType |\n|---|---|---|---|---|\n| 1005 | 350.00 | 359.98 | -9.98 | Stored total is UNDER-STATED |\n\n**Summary:**\n| MismatchedOrders | StoredRevenue | CalculatedRevenue | TotalExposure |\n|---|---|---|---|\n| 1 | 350.00 | 359.98 | -9.98 |\n\n---\n\n## Explanation\n\n### Why you need a rounding tolerance\nFloating-point arithmetic inside a database engine can introduce tiny errors \u2014\nfor example, `199.99 \u00d7 2` might produce `399.9799999` instead of a clean `399.98`.\nThese are machine-level precision artefacts, not real data problems. Using\n`ABS(...) > 0.01` ignores any difference smaller than 1 cent, filtering out\nrounding noise while still catching genuine mismatches like the 9.98 discrepancy\nin OrderID 1005.\n\n### Integer division trap \u2014 why 100.0 matters\nIn SQL Server, dividing an integer by an integer performs integer division and\ntruncates the decimal. So `DiscountPct / 100` where DiscountPct = 10 gives 0,\nnot 0.1. Writing `100.0` (or `CAST(DiscountPct AS DECIMAL(5,2)) / 100.0`) forces\nthe engine to use decimal arithmetic and return 0.1. This is one of the most\ncommon silent bugs in SQL financial calculations.\n\n### ABS() \u2014 catching errors in both directions\n`ABS()` returns the absolute (positive) value of any number. Using it in the WHERE\nclause means you catch both over-statements (stored total is too high) and\nunder-statements (stored total is too low) with a single expression, instead of\nwriting two separate conditions. The CASE WHEN in the SELECT then labels which\ndirection the error went, which matters for understanding the root cause.\n\n---\n\n## Interview Angle\n*\"Why store a calculated field at all if you can always compute it?\"*\nPerformance \u2014 computing totals on every query is expensive at scale.\nBut stored calculated fields MUST be validated at load time because any mismatch\ncreates a permanent lie in the data. The alternative is a computed column in SQL Server\n(`AS Quantity * UnitPrice * (1 - DiscountPct / 100.0) PERSISTED`) which is\nalways consistent by design.\n\n---\n\n## Severity: **Critical**\nRevenue mis-statements affect P&L reporting, audit sign-off, and commission calculations.\n",
  "M06_Consistency/M06-E2_cross_column_logic": "# M06 \u2014 Consistency | Example 2: Cross-Column Logic Violations\n\n## The Question\n> Beyond computed fields, business logic often ties multiple columns together in rules\n> that can't be expressed with a simple range check.\n> Your BA asks: **\"Validate the cross-column logic in our Orders and Products tables:\n> a cancelled order should never have a ship date;\n> a delivered order must have a ship date;\n> an active product with zero stock needs a reorder flag;\n> and a product can't be in a sub-category without a parent category.\"**\n\n---\n\n## Table References\n**Orders** \u2014 `schemas/02_Orders.csv`\n**Products** \u2014 `schemas/03_Products.csv`\n\n---\n\n## Solution Query\n\n```sql\n-- Orders: cross-column status vs date logic\nSELECT\n    OrderID,\n    Status,\n    OrderDate,\n    ShipDate,\n    CASE\n        WHEN Status = 'Cancelled'  AND ShipDate IS NOT NULL\n            THEN 'Cancelled order has a ship date \u2014 contradictory'\n        WHEN Status = 'Delivered'  AND ShipDate IS NULL\n            THEN 'Delivered order has no ship date \u2014 missing'\n        WHEN Status = 'Returned'   AND ShipDate IS NULL\n            THEN 'Returned order has no ship date \u2014 missing'\n        WHEN Status = 'Shipped'    AND ShipDate IS NULL\n            THEN 'Shipped order has no ship date \u2014 missing'\n        WHEN Status NOT IN ('Pending','Processing','Shipped','Delivered','Returned','Cancelled')\n            THEN 'Unrecognised status value: ' + Status\n        ELSE 'OK'\n    END AS LogicViolation\nFROM Orders\nWHERE\n    (Status = 'Cancelled'  AND ShipDate IS NOT NULL)\n OR (Status IN ('Delivered','Returned','Shipped') AND ShipDate IS NULL)\n OR Status NOT IN ('Pending','Processing','Shipped','Delivered','Returned','Cancelled');\n\n-- Products: stock vs active status, cost vs price, category completeness\nSELECT\n    ProductID,\n    ProductName,\n    Category,\n    SubCategory,\n    UnitCost,\n    SellingPrice,\n    StockQty,\n    MinStockLevel,\n    IsActive,\n    CASE\n        WHEN IsActive = 1 AND StockQty <= MinStockLevel\n            THEN 'Active product at or below reorder level \u2014 needs restock'\n        WHEN IsActive = 0 AND StockQty > 0\n            THEN 'Inactive product still has stock \u2014 investigate'\n        WHEN SubCategory IS NOT NULL AND Category IS NULL\n            THEN 'SubCategory set but Category is NULL \u2014 hierarchy broken'\n        WHEN ProductName IS NULL OR LTRIM(RTRIM(ISNULL(ProductName,''))) = ''\n            THEN 'Product has no name'\n        ELSE 'OK'\n    END AS LogicViolation\nFROM Products\nWHERE\n    (IsActive = 1 AND StockQty <= MinStockLevel)\n OR (IsActive = 0 AND StockQty > 0)\n OR (SubCategory IS NOT NULL AND Category IS NULL)\n OR (ProductName IS NULL OR LTRIM(RTRIM(ISNULL(ProductName,''))) = '');\n```\n\n---\n\n## Expected Output\n\n**Orders cross-column:**\n| OrderID | Status | ShipDate | LogicViolation |\n|---|---|---|---|\n| 1003 | Pending | NULL | OK \u2014 Pending orders may not have ship date yet |\n| (none in our data match the cancel+shipdate rule) | | | |\n\n**Products cross-column:**\n| ProductID | ProductName | Category | SubCategory | StockQty | MinStockLevel | LogicViolation |\n|---|---|---|---|---|---|---|\n| P008 | Keyboard Mech | Electronics | Peripherals | 0 | 10 | Active product at or below reorder level |\n| P011 | NULL | Electronics | Peripherals | 50 | 10 | Product has no name |\n| P012 | Docking Station | Electronics | NULL | 20 | 5 | OK (SubCategory null but Category present) |\n| P013 | Mousepad XL | Electronics | Peripherals | -5 | 20 | Active product at or below reorder level |\n\n---\n\n## Explanation\n\n### What makes a cross-column rule different\nA cross-column validation cannot be reduced to a single-column check because\nthe problem only appears when you look at two columns together. A ShipDate of\n\"2024-01-19\" is a perfectly valid date on its own. But paired with an OrderDate of\n\"2024-01-22\", it becomes impossible. The `CASE WHEN` chain evaluates conditions\nusing multiple column references simultaneously \u2014 that is what makes it a cross-column\nrule rather than a simple range check.\n\n### Layering CASE WHEN with WHERE\nThe CASE WHEN in the SELECT labels what the problem is; the WHERE clause filters to\nonly the rows that have any problem. They work in tandem: the WHERE ensures you are\nonly returning rows worth investigating, and the CASE label tells you exactly which\nrule each row broke. Without the label, you would know a row is bad but not why.\n\n### The difference between a data error and a business alert\n`StockQty <= MinStockLevel` is not a data error \u2014 the stock figure itself is accurate.\nIt is a **business alert**: the data is correct, but it signals that an operational\naction is needed (raise a purchase order). Both types are found using identical SQL\npatterns, so the CASE label or the column header on the report is what communicates\nto the reader whether they need to fix the data or take a business action.\n\n---\n\n## Interview Angle\n*\"How do you validate business rules that span multiple columns?\"*\nCASE WHEN with multi-column conditions in both the SELECT (to label the issue) and\nWHERE (to filter to problem rows). For ongoing enforcement, SQL Server CHECK constraints\ncan reference multiple columns: `CHECK (ShipDate IS NULL OR ShipDate >= OrderDate)`.\n\n---\n\n## Severity: **High**\nLogic contradictions corrupt operational dashboards and SLA calculations.\n",
  "M07_Business_Rules/M07-E1_allowed_values_domain_check": "# M07 \u2014 Business Rules | Example 1: Allowed Values (Domain / Lookup Validation)\n\n## The Question\n> The data governance team has defined a list of valid values for categorical fields \u2014\n> called a **reference data domain**. Any value outside this approved list is invalid.\n> **\"Check the Employees table for department names that don't match the approved list,\n> and check the Orders table for unrecognised Status values.\n> Return the bad values so we can decide whether to map or reject them.\"**\n\n---\n\n## Table References\n**Employees** \u2014 `schemas/04_Employees.csv` \u2014 Department must be one of: Sales, Technology, HR, Finance, Marketing\n**Orders** \u2014 `schemas/02_Orders.csv` \u2014 Status must be one of: Pending, Processing, Shipped, Delivered, Returned, Cancelled\n\n---\n\n## Solution Query\n\n```sql\n-- Method A: Hard-coded allowed list (good for small, stable domains)\nSELECT\n    EmployeeID,\n    FullName,\n    Department,\n    'Department not in approved list' AS Violation\nFROM Employees\nWHERE Department NOT IN ('Sales', 'Technology', 'HR', 'Finance', 'Marketing')\n   OR Department IS NULL;\n\n-- Method B: Reference table lookup (better for larger or changing domains)\n-- (Imagine a table: DomainValues (DomainName, AllowedValue))\n-- SELECT e.EmployeeID, e.FullName, e.Department\n-- FROM Employees e\n-- LEFT JOIN DomainValues d\n--     ON d.DomainName = 'Department' AND d.AllowedValue = e.Department\n-- WHERE d.AllowedValue IS NULL;\n\n-- Orders: Status domain check with frequency count of bad values\nSELECT\n    Status,\n    COUNT(*) AS Occurrences,\n    'Invalid status value' AS Violation\nFROM Orders\nWHERE Status NOT IN ('Pending','Processing','Shipped','Delivered','Returned','Cancelled')\nGROUP BY Status\nORDER BY Occurrences DESC;\n\n-- Combined domain health summary\nSELECT 'Employees.Department' AS TableField,\n    COUNT(*) AS TotalRows,\n    SUM(CASE WHEN Department NOT IN ('Sales','Technology','HR','Finance','Marketing')\n             OR Department IS NULL THEN 1 ELSE 0 END) AS InvalidCount,\n    CAST(\n        SUM(CASE WHEN Department NOT IN ('Sales','Technology','HR','Finance','Marketing')\n                  OR Department IS NULL THEN 1 ELSE 0 END) * 100.0 / COUNT(*)\n    AS DECIMAL(5,2)) AS InvalidPct\nFROM Employees\nUNION ALL\nSELECT 'Orders.Status',\n    COUNT(*),\n    SUM(CASE WHEN Status NOT IN ('Pending','Processing','Shipped','Delivered','Returned','Cancelled')\n             THEN 1 ELSE 0 END),\n    CAST(\n        SUM(CASE WHEN Status NOT IN ('Pending','Processing','Shipped','Delivered','Returned','Cancelled')\n                 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)\n    AS DECIMAL(5,2))\nFROM Orders;\n```\n\n---\n\n## Expected Output\n\n**Employees \u2014 Dept violations:**\n(No violations in our sample data \u2014 all departments are valid.)\n\n**Domain health summary:**\n| TableField | TotalRows | InvalidCount | InvalidPct |\n|---|---|---|---|\n| Employees.Department | 18 | 0 | 0.00% |\n| Orders.Status | 20 | 0 | 0.00% |\n\n---\n\n## Explanation\n\n### Domain validation vs format and range validation\nIt helps to distinguish three similar-sounding checks. **Format validation** asks:\n\"is the shape of this value correct?\" (does the email have an @ sign?). **Range\nvalidation** asks: \"is this number within acceptable bounds?\" (is quantity > 0?).\n**Domain validation** asks: \"does this value appear on the approved list?\" (is this\ndepartment one of the five we recognise?). All three are needed; none of them replaces\nthe others.\n\n### The NULL trap inside NOT IN\n`NULL NOT IN ('Sales', 'Technology', ...)` does not return TRUE \u2014 it evaluates to\nUNKNOWN, because SQL cannot confirm that NULL is different from any list member.\nThis means a row with a NULL department silently passes the NOT IN check without\nbeing flagged. The fix is explicit: always add `OR Department IS NULL` as a separate\ncondition alongside the NOT IN check.\n\n### Why a reference table beats a hard-coded list\nA hard-coded `NOT IN ('Sales', 'Technology', ...)` list must be updated manually in\nevery script that uses it whenever the business adds or renames a department. A\n`DomainValues` reference table externalises the approved list into data. You update\none table row and every validation query that joins to it automatically reflects the\nchange. This is the production-grade approach \u2014 it also allows non-technical users\nto maintain the reference data without touching SQL code.\n\n---\n\n## Interview Angle\n*\"How would you design a reusable domain validation system in SQL?\"*\nCreate a `DomainValues` reference table with `(DomainName, AllowedValue)`.\nValidate with a LEFT JOIN: rows where `AllowedValue IS NULL` after the join have invalid values.\nThis pattern scales to hundreds of domains without touching validation queries.\n\n---\n\n## Severity: **High**\nInvalid domain values break GROUP BY reports (unexpected groups appear),\nPower BI slicers (unknown categories), and ETL LOOKUP transformations (no match = row dropped or errored).\n",
  "M08_Reconciliation/M08-E1_source_to_target_count_and_sum": "# M08 \u2014 Reconciliation | Example 1: Source-to-Target Count and Sum Reconciliation\n\n## The Question\n> After an ETL pipeline runs overnight, the data warehouse team needs to confirm\n> that nothing was lost or duplicated in transit.\n> **\"Write a reconciliation query that compares the order totals between the\n> staging (source) and the target Orders table. Check row counts, total revenue,\n> and identify any orders that exist in source but not in target, and vice versa.\"**\n\n---\n\n## Table Reference\n**Orders** \u2014 `schemas/02_Orders.csv` (treated as the SOURCE / staging table)\n\nFor this exercise, imagine `Orders_DW` is the target data warehouse table \u2014\nwe simulate it by filtering Orders to a subset (e.g., only Delivered orders were loaded).\n\n---\n\n## Solution Query\n\n```sql\n-- Simulate: source = all Orders, target = only Delivered/Shipped orders\n-- (Represents a partial load scenario)\n\n-- Step 1: High-level count and sum comparison\nSELECT 'SOURCE (Staging)'         AS DataSet,\n    COUNT(*)                      AS RowCount,\n    SUM(TotalAmount)              AS TotalRevenue,\n    MIN(OrderDate)                AS EarliestDate,\n    MAX(OrderDate)                AS LatestDate\nFROM Orders\n\nUNION ALL\n\nSELECT 'TARGET (Warehouse \u2014 Delivered only)',\n    COUNT(*),\n    SUM(TotalAmount),\n    MIN(OrderDate),\n    MAX(OrderDate)\nFROM Orders\nWHERE Status IN ('Delivered','Returned');   -- simulating the DW load filter\n\n-- Step 2: Identify records in SOURCE but NOT in TARGET (missing from load)\nSELECT\n    o.OrderID,\n    o.CustomerID,\n    o.OrderDate,\n    o.TotalAmount,\n    o.Status,\n    'In SOURCE \u2014 missing from TARGET' AS ReconciliationFlag\nFROM Orders o\nWHERE o.Status NOT IN ('Delivered','Returned')   -- these \"weren't loaded\"\n  AND o.Status NOT IN ('Cancelled')              -- cancelled may legitimately be excluded\nORDER BY o.OrderID;\n\n-- Step 3: Variance report \u2014 difference in key aggregates\nSELECT\n    'Row Count Variance'          AS Metric,\n    COUNT(*) - (SELECT COUNT(*) FROM Orders WHERE Status IN ('Delivered','Returned'))\n        AS Variance\nFROM Orders\nUNION ALL\nSELECT\n    'Revenue Variance',\n    SUM(TotalAmount) - (SELECT SUM(TotalAmount) FROM Orders WHERE Status IN ('Delivered','Returned'))\nFROM Orders;\n\n-- Step 4: Control total \u2014 match to source system grand total\n-- In real ETL: compare this to a control record sent by the source\nSELECT\n    CONVERT(VARCHAR, CAST(MIN(OrderDate) AS DATE), 103) AS PeriodStart,\n    CONVERT(VARCHAR, CAST(MAX(OrderDate) AS DATE), 103) AS PeriodEnd,\n    COUNT(*)                    AS TotalOrders,\n    SUM(Quantity)               AS TotalUnits,\n    SUM(TotalAmount)            AS TotalRevenue,\n    AVG(TotalAmount)            AS AvgOrderValue\nFROM Orders;\n```\n\n---\n\n## Expected Output\n\n**Step 1 (high-level):**\n| DataSet | RowCount | TotalRevenue |\n|---|---|---|\n| SOURCE (Staging) | 20 | ~5,338.71 |\n| TARGET (Warehouse \u2014 Delivered only) | 13 | ~3,898.73 |\n\n**Step 3 (variance):**\n| Metric | Variance |\n|---|---|\n| Row Count Variance | 7 |\n| Revenue Variance | ~1,439.98 |\n\n---\n\n## Explanation\n\n### The three pillars of a control total reconciliation\nEvery ETL reconciliation checks three things. First, **row count**: did the same\nnumber of records arrive in the target as were sent from the source? A lower count\nmeans rows were dropped (by a filter, a failed lookup, or an error handler). A higher\ncount means rows were duplicated. Second, **financial sum**: does the total revenue\nin the target match the source? Even if row counts match, individual amounts could\nhave been corrupted. Third, **date range**: does the min/max date in the target cover\nthe expected period? A shifted or truncated range means the wrong period was loaded.\nAll three must agree before the data is signed off for reporting.\n\n### CONVERT date formatting for readable outputs\n`CONVERT(VARCHAR, date, 103)` formats a date as `DD/MM/YYYY` \u2014 the British date\nconvention. The style code is the third argument: 101 gives US format `MM/DD/YYYY`,\n120 gives ISO format `YYYY-MM-DD HH:MM:SS`, 112 gives `YYYYMMDD` (useful for\nfile-naming). Use the style that matches your audience or output format requirement.\n\n### How control files work in real ETL pipelines\nIn a production ETL, the source system does not just send data \u2014 it also sends a\n**control file** or **trailer record** containing the expected row count and sum for\nthat batch. The ETL loads the data, then queries the loaded table and compares its\nown count and sum to the control figures. If they match, the load is confirmed and\ndownstream jobs are released. If they differ by any amount, the load is failed and\nan alert is raised before any report consumer sees the data.\n\n---\n\n## Interview Angle\n*\"What is a control total and why does ETL need one?\"*\nA control total is a pre-agreed count or sum from the source system. The ETL loads\nthe data, then compares its own count/sum to the control. If they match, the load is\nconfirmed complete. If not, the ETL fails and alerts the team \u2014 preventing corrupted\ndata from reaching the warehouse.\n\n---\n\n## Severity: **Critical**\nMissed or duplicated records in a data load invalidate all downstream reports\nand can take hours to detect and correct after business users have already acted on wrong numbers.\n",
  "M09_Temporal/M09-E1_gap_and_overlap_detection": "# M09 \u2014 Temporal | Example 1: Gap and Overlap Detection in Date Sequences\n\n## The Question\n> The finance team tracks sales targets by quarter, and the HR system tracks employment\n> periods. Both require contiguous date coverage with no gaps and no overlaps.\n> **\"Check the SalesTargets table for any employee who has a gap in their quarterly\n> coverage (e.g., Q1 recorded but Q2 missing), and check the Transactions table\n> for any days where no transactions were recorded (potential system outage or data gap).\"**\n\n---\n\n## Table References\n**SalesTargets** \u2014 `schemas/06_SalesTargets.csv`\n**Transactions** \u2014 `schemas/05_Transactions.csv`\n\n---\n\n## Solution Query\n\n```sql\n-- Check 1: Missing quarter coverage per employee using LAG()\n-- (Detects: Q1 recorded, Q3 recorded, Q2 missing)\nWITH QuarterlyRank AS (\n    SELECT\n        EmployeeID,\n        Year,\n        Quarter,\n        ROW_NUMBER() OVER (PARTITION BY EmployeeID, Year ORDER BY Quarter) AS QuarterSeq\n    FROM SalesTargets\n    GROUP BY EmployeeID, Year, Quarter\n),\nLagged AS (\n    SELECT\n        EmployeeID,\n        Year,\n        Quarter,\n        QuarterSeq,\n        LAG(Quarter) OVER (PARTITION BY EmployeeID, Year ORDER BY Quarter) AS PrevQuarter\n    FROM QuarterlyRank\n)\nSELECT\n    EmployeeID,\n    Year,\n    PrevQuarter AS LastQuarterRecorded,\n    Quarter     AS NextQuarterRecorded,\n    'Gap detected between ' + PrevQuarter + ' and ' + Quarter AS GapFlag\nFROM Lagged\nWHERE PrevQuarter IS NOT NULL\n  AND CAST(RIGHT(Quarter,1) AS INT) - CAST(RIGHT(PrevQuarter,1) AS INT) > 1;\n\n-- Check 2: Transaction date gaps \u2014 days with no transactions\n-- Generate a calendar of expected dates, then find which ones have no transactions\nWITH DateRange AS (\n    SELECT MIN(CAST(TransactionDate AS DATE)) AS StartDate,\n           MAX(CAST(TransactionDate AS DATE)) AS EndDate\n    FROM Transactions\n    WHERE TransactionDate IS NOT NULL\n),\nCalendar AS (\n    SELECT CAST(StartDate AS DATE) AS CalDate\n    FROM DateRange\n    UNION ALL\n    SELECT DATEADD(DAY, 1, CalDate)\n    FROM Calendar\n    JOIN DateRange ON CalDate < EndDate\n)\nSELECT\n    c.CalDate AS MissingDate,\n    DATENAME(WEEKDAY, c.CalDate) AS DayOfWeek,\n    'No transactions recorded on this date' AS Flag\nFROM Calendar c\nLEFT JOIN (\n    SELECT DISTINCT CAST(TransactionDate AS DATE) AS TxnDate\n    FROM Transactions\n    WHERE TransactionDate IS NOT NULL\n) t ON c.CalDate = t.TxnDate\nWHERE t.TxnDate IS NULL\nOPTION (MAXRECURSION 1000);\n\n-- Check 3: Duplicate reference numbers in Transactions (same reference, multiple rows)\nSELECT\n    ReferenceNo,\n    COUNT(*) AS Occurrences,\n    COUNT(DISTINCT AccountID) AS UniqueAccounts,\n    MIN(TransactionDate) AS FirstDate,\n    MAX(TransactionDate) AS LastDate,\n    'Duplicate reference number' AS Flag\nFROM Transactions\nWHERE ReferenceNo IS NOT NULL\nGROUP BY ReferenceNo\nHAVING COUNT(*) > 1;\n```\n\n---\n\n## Expected Output\n\n**Check 2 (date gaps):**\n| MissingDate | DayOfWeek | Flag |\n|---|---|---|\n| 2024-03-04 | Monday | No transactions recorded on this date |\n| 2024-03-06 | Wednesday | No transactions recorded on this date |\n| 2024-03-11 | Monday | No transactions recorded on this date |\n| ... | ... | ... |\n\n**Check 3 (duplicate references):**\n| ReferenceNo | Occurrences | UniqueAccounts | Flag |\n|---|---|---|---|\n| REF10006 | 2 | 2 | Duplicate reference number |\n| REF10014 | 2 | 2 | Duplicate reference number |\n\n---\n\n## Explanation\n\n### LAG() \u2014 looking back one row to find a skip\n`LAG(column) OVER (PARTITION BY ... ORDER BY ...)` returns the value from the\nprevious row within a defined group. Here it returns the previous quarter for the\nsame employee and year. Subtracting the previous quarter number from the current\nquarter number (extracted with `RIGHT(Quarter, 1)` and cast to INT) tells you how\nmany quarters apart they are. A difference of 2 or more means at least one quarter\nwas skipped \u2014 a gap in the coverage sequence.\n\n### Generating a complete date calendar with a Recursive CTE\nThe standard SQL pattern for producing a continuous series of dates is a Recursive CTE.\nThe **anchor** selects the first date. Each **recursive step** adds one day using\n`DATEADD(DAY, 1, CalDate)` and continues until the end date is reached. Once you have\nthis calendar, a LEFT JOIN against the actual data reveals any date with no matching\ntransaction \u2014 those are your gaps. This pattern works for any sequence: dates, months,\ninvoice numbers, sequence IDs.\n\n### Why duplicate reference numbers are a temporal problem\nA reference number is designed to uniquely identify one event at one point in time.\nWhen the same reference number appears on two rows \u2014 especially across two different\naccounts or two different dates \u2014 it signals either a reprocessing error (the same\nfile was loaded twice) or a duplicate payment. It is classed as a temporal integrity\nissue because the problem involves event identity over time, not just a static value\nconstraint.\n\n---\n\n## Interview Angle\n*\"How do you detect gaps in a time series in SQL?\"*\nTwo approaches: (1) use LAG() to compare each row's date to the previous row's date \u2014\nif the difference is more than the expected interval, there is a gap; (2) generate a\ncomplete date calendar with a Recursive CTE and LEFT JOIN the actual data against it \u2014\ndates with no matching record are gaps.\n\n---\n\n## Severity: **High**\nDate gaps in financial transaction logs may indicate system downtime or missing data\nloads. Duplicate reference numbers may indicate double-payment.\n",
  "M10_Statistical/M10-E1_outlier_detection": "# M10 \u2014 Statistical | Example 1: Outlier Detection Using Z-Score and IQR\n\n## The Question\n> The compliance team wants to flag unusually large financial transactions for\n> manual review. Simple range checks won't work because the \"normal\" range changes\n> depending on the account type. They ask:\n> **\"Use statistical methods to identify transactions whose amounts are\n> significantly higher or lower than what is typical for our dataset.\n> Apply both a Z-Score method and an IQR-based method and compare the results.\"**\n\n---\n\n## Table Reference\n**Transactions** \u2014 `schemas/05_Transactions.csv`\n\nKnown outlier: TXN0013 has Amount = 99,999,999.99 \u2014 a statistical extreme.\n\n---\n\n## Solution Query\n\n```sql\n-- Method 1: Z-Score \u2014 flags values more than 2 standard deviations from the mean\n-- Z = (value - mean) / standard_deviation\n-- |Z| > 2 = unusual, |Z| > 3 = extreme outlier\nWITH Stats AS (\n    SELECT\n        AVG(Amount)          AS MeanAmount,\n        STDEV(Amount)        AS StdDevAmount\n    FROM Transactions\n    WHERE Amount IS NOT NULL\n      AND Amount <> 0        -- exclude zero-amount transactions from stats\n)\nSELECT\n    t.TransactionID,\n    t.AccountID,\n    t.Amount,\n    s.MeanAmount,\n    s.StdDevAmount,\n    CAST((t.Amount - s.MeanAmount) / NULLIF(s.StdDevAmount, 0) AS DECIMAL(8,2)) AS ZScore,\n    CASE\n        WHEN ABS((t.Amount - s.MeanAmount) / NULLIF(s.StdDevAmount, 0)) > 3\n            THEN 'EXTREME outlier (|Z| > 3)'\n        WHEN ABS((t.Amount - s.MeanAmount) / NULLIF(s.StdDevAmount, 0)) > 2\n            THEN 'Outlier (|Z| > 2)'\n        ELSE 'Normal'\n    END AS ZScoreFlag\nFROM Transactions t\nCROSS JOIN Stats s\nWHERE ABS((t.Amount - s.MeanAmount) / NULLIF(s.StdDevAmount, 0)) > 2\nORDER BY ABS((t.Amount - s.MeanAmount) / NULLIF(s.StdDevAmount, 0)) DESC;\n\n-- Method 2: IQR (Interquartile Range) \u2014 more robust to extreme skew\n-- Lower fence = Q1 - 1.5 * IQR\n-- Upper fence = Q3 + 1.5 * IQR\nWITH Quartiles AS (\n    SELECT\n        PERCENTILE_CONT(0.25) WITHIN GROUP (ORDER BY Amount) OVER () AS Q1,\n        PERCENTILE_CONT(0.75) WITHIN GROUP (ORDER BY Amount) OVER () AS Q3\n    FROM Transactions\n    WHERE Amount IS NOT NULL\n),\nIQR_Bounds AS (\n    SELECT DISTINCT\n        Q1,\n        Q3,\n        Q3 - Q1                     AS IQR,\n        Q1 - 1.5 * (Q3 - Q1)       AS LowerFence,\n        Q3 + 1.5 * (Q3 - Q1)       AS UpperFence\n    FROM Quartiles\n)\nSELECT\n    t.TransactionID,\n    t.AccountID,\n    t.Amount,\n    b.LowerFence,\n    b.UpperFence,\n    CASE\n        WHEN t.Amount < b.LowerFence THEN 'Below lower fence \u2014 unusually small'\n        WHEN t.Amount > b.UpperFence THEN 'Above upper fence \u2014 unusually large'\n    END AS IQR_Flag\nFROM Transactions t\nCROSS JOIN IQR_Bounds b\nWHERE t.Amount < b.LowerFence\n   OR t.Amount > b.UpperFence\nORDER BY t.Amount DESC;\n\n-- Method 3: Simple percentile banding \u2014 quick executive summary\nSELECT\n    CASE\n        WHEN Amount < 0                                     THEN 'Negative'\n        WHEN Amount = 0                                     THEN 'Zero'\n        WHEN Amount BETWEEN 0.01    AND 500                 THEN 'Low (0\u2013500)'\n        WHEN Amount BETWEEN 500.01  AND 2000               THEN 'Medium (500\u20132K)'\n        WHEN Amount BETWEEN 2000.01 AND 10000              THEN 'High (2K\u201310K)'\n        WHEN Amount > 10000                                 THEN 'Very High (>10K)'\n    END AS AmountBand,\n    COUNT(*) AS TransactionCount,\n    SUM(Amount) AS TotalAmount\nFROM Transactions\nGROUP BY\n    CASE\n        WHEN Amount < 0                                     THEN 'Negative'\n        WHEN Amount = 0                                     THEN 'Zero'\n        WHEN Amount BETWEEN 0.01    AND 500                 THEN 'Low (0\u2013500)'\n        WHEN Amount BETWEEN 500.01  AND 2000               THEN 'Medium (500\u20132K)'\n        WHEN Amount BETWEEN 2000.01 AND 10000              THEN 'High (2K\u201310K)'\n        WHEN Amount > 10000                                 THEN 'Very High (>10K)'\n    END\nORDER BY MIN(Amount);\n```\n\n---\n\n## Expected Output\n\n**Method 1 (Z-Score):**\n| TransactionID | Amount | ZScore | ZScoreFlag |\n|---|---|---|---|\n| TXN0013 | 99,999,999.99 | ~22.36 | EXTREME outlier (|Z| > 3) |\n\n**Method 3 (banding):**\n| AmountBand | TransactionCount | TotalAmount |\n|---|---|---|\n| Negative | 1 | -150.00 |\n| Zero | 1 | 0.00 |\n| Low (0\u2013500) | 10 | 2,530.50 |\n| Medium (500\u20132K) | 5 | 4,350.00 |\n| High (2K\u201310K) | 2 | 7,200.00 |\n| Very High (>10K) | 1 | 99,999,999.99 |\n\n---\n\n## Explanation\n\n### Z-Score \u2014 how far is this value from normal?\nA Z-Score expresses how many standard deviations a value sits away from the mean.\nThe formula is `(value \u2212 mean) / standard deviation`. A Z-Score of 0 means the\nvalue is exactly average. Scores between \u22122 and +2 cover roughly 95% of a normally\ndistributed dataset. Anything outside \u00b12 is flagged as unusual; outside \u00b13 is\nconsidered extreme. `NULLIF(StdDevAmount, 0)` guards against a division-by-zero\nerror in the rare case where all amounts are identical and the standard deviation is zero.\n\n### The limitation of Z-Score on skewed data\nZ-Score has a weakness: a single extreme outlier inflates both the mean and the\nstandard deviation, which can actually make other outliers look less extreme than\nthey are. TXN0013 with 99 million pushes the standard deviation so high that a\ntransaction of 15,000 might no longer show as unusual. This is why the IQR method\nexists as an alternative.\n\n### IQR \u2014 a more robust outlier boundary\nThe Interquartile Range (IQR) uses the middle 50% of the data rather than the mean\nand standard deviation. Q1 is the value at the 25th percentile; Q3 is at the 75th.\nIQR = Q3 \u2212 Q1. The **lower fence** is `Q1 \u2212 1.5 \u00d7 IQR`; the **upper fence** is\n`Q3 + 1.5 \u00d7 IQR`. Any value outside these fences is a statistical outlier. Because\nit is based on percentiles rather than averages, extreme values have almost no effect\non where the fences sit \u2014 making IQR far more reliable on skewed financial data.\n\n### PERCENTILE_CONT \u2014 computing percentiles in SQL\n`PERCENTILE_CONT(0.25) WITHIN GROUP (ORDER BY Amount) OVER ()` calculates the 25th\npercentile across the entire dataset. The `WITHIN GROUP (ORDER BY ...)` clause specifies\nthe sort order used to find the percentile position. The `OVER ()` with no partition\nmeans it applies to the whole result set. This is a window function available in\nSQL Server 2012 and later.\n\n---\n\n## Interview Angle\n*\"What is a Z-Score and when would you use it for data validation?\"*\nA Z-Score converts a value to the number of standard deviations from the mean.\nValues with |Z| > 2 are flagged for review. Use it for anomaly detection in financial\ndata, call duration monitoring, or any numeric field where extreme values indicate\nerrors or fraud. Prefer IQR when the data has a heavily skewed distribution.\n\n---\n\n## Severity: **High**\nStatistical outliers in financial data are a primary indicator of data entry errors,\nsystem bugs, or fraud. Missing them can lead to regulatory penalties.\n"
}

SCHEMAS = {
  "01_Customers.csv": "CustomerID,FirstName,LastName,Email,Phone,DOB,Country,PostalCode,CreatedDate,IsActive\n1,James,Carter,james.carter@email.com,+44-7911-123456,1985-03-12,UK,EC1A 1BB,2021-01-15,1\n2,Priya,Nair,priya.nair@email.com,+91-9876543210,1990-07-22,India,560001,2021-02-10,1\n3,Mohammed,Al-Rashid,,+971-501234567,1978-11-05,UAE,00000,2021-03-01,1\n4,Sara,O'Brien,sara.obrien@email.com,,1995-04-18,Ireland,D02 XY45,2021-03-15,1\n5,Chen,Wei,chen.wei@email.com,+86-13812345678,,China,200000,2021-04-01,1\n6,Anjali,Sharma,ANJALI.SHARMA@EMAIL.COM,+91-9988776655,1988-09-30,India,110001,2021-04-20,1\n7,Tom,Brooks,tom.brooks,+44-7922-654321,1975-12-01,UK,SW1A 2AA,2021-05-05,1\n8,Fatima,Hassan,fatima.hassan@email.com,+971-509876543,2005-06-14,UAE,00000,2021-05-18,1\n9,David,Kim,david.kim@email.com,+82-10-1234-5678,1983-02-28,Korea,06000,2021-06-01,0\n10,Elena,Popescu,elena.popescu@email.com,+40-721-234567,1991-08-19,Romania,010101,2021-06-15,1\n11,James,Carter,james.carter@email.com,+44-7911-123456,1985-03-12,UK,EC1A 1BB,2021-07-01,1\n12,Ravi,Kumar,,+91-9123456789,1987-05-25,India,400001,2021-07-20,1\n13,Anna,M\u00fcller,anna.muller@email.com,+49-170-1234567,1993-10-11,Germany,10115,2021-08-01,1\n14,Carlos,Ruiz,carlos.ruiz@email.com,+34-612-345678,,Spain,28001,2021-08-15,1\n15,Lily,Zhang,lily.zhang@email.com,+86-13987654321,2010-03-07,China,100000,2021-09-01,1\n16,N/A,N/A,N/A,N/A,N/A,N/A,N/A,2021-09-10,1\n17,Omar,Farouq,omar.farouq@email.com,+20-100-1234567,1980-01-01,Egypt,11511,2021-10-01,1\n18,Sophie,Dubois,sophie.dubois@email.com,+33-612-345678,1996-12-25,France,75001,2021-10-20,1\n19,Ahmed,Khan,ahmed.khan@email.com,99999999999,1972-04-15,Pakistan,75600,2021-11-01,1\n20,Mei,Lin,mei.lin@email.com,+86-13611223344,1989-07-04,China,310000,2021-11-15,1\n",
  "02_Orders.csv": "OrderID,CustomerID,ProductID,OrderDate,ShipDate,Quantity,UnitPrice,TotalAmount,Status,DiscountPct\n1001,1,P001,2024-01-10,2024-01-15,2,49.99,99.98,Delivered,0\n1002,2,P002,2024-01-12,2024-01-18,1,199.99,199.99,Delivered,0\n1003,3,P001,2024-01-15,,3,49.99,149.97,Pending,0\n1004,99,P003,2024-01-18,2024-01-20,1,299.99,299.99,Delivered,0\n1005,5,P002,2024-01-20,2024-01-25,2,199.99,350.00,Delivered,10\n1006,6,P004,2024-01-22,2024-01-19,1,89.99,89.99,Delivered,0\n1007,7,P001,2024-01-25,2024-01-30,0,49.99,0.00,Cancelled,0\n1008,8,P005,2024-02-01,2024-02-06,1,499.99,499.99,Delivered,0\n1009,9,P003,2024-02-03,2024-02-08,2,299.99,599.98,Delivered,0\n1010,10,P002,2024-02-05,2024-02-10,1,199.99,199.99,Delivered,0\n1011,1,P006,2024-02-08,,2,75.00,150.00,Shipped,0\n1012,2,P001,2024-02-10,2024-02-14,3,49.99,149.97,Delivered,0\n1013,3,P007,2024-02-12,2024-02-17,1,125.00,125.00,Delivered,0\n1014,4,P002,2024-02-15,2024-02-20,2,199.99,399.98,Delivered,0\n1015,5,P003,2024-02-18,2024-02-23,1,299.99,299.99,Returned,0\n1016,11,P001,2024-02-20,2024-02-25,5,49.99,249.95,Delivered,0\n1017,12,P004,2024-02-22,,1,89.99,89.99,Processing,0\n1018,6,P005,2024-02-25,2024-03-01,1,499.99,499.99,Delivered,0\n1019,7,P002,2023-03-01,2024-03-06,2,199.99,399.98,Delivered,0\n1020,8,P008,2024-03-05,2024-03-10,1,-50.00,-50.00,Delivered,0\n",
  "03_Products.csv": "ProductID,ProductName,Category,SubCategory,UnitCost,SellingPrice,StockQty,MinStockLevel,IsActive,SupplierID\nP001,Wireless Mouse,Electronics,Peripherals,22.00,49.99,150,20,1,SUP001\nP002,USB-C Hub,Electronics,Peripherals,85.00,199.99,75,15,1,SUP001\nP003,Standing Desk,Furniture,Office,120.00,299.99,30,5,1,SUP002\nP004,Desk Lamp,Furniture,Lighting,35.00,89.99,60,10,1,SUP002\nP005,Monitor 27\",Electronics,Displays,200.00,499.99,25,5,1,SUP003\nP006,Webcam HD,Electronics,Peripherals,30.00,75.00,80,15,1,SUP001\nP007,Laptop Stand,Furniture,Accessories,50.00,125.00,45,10,1,SUP002\nP008,Keyboard Mech,Electronics,Peripherals,90.00,220.00,0,10,1,SUP001\nP009,Headset Pro,Electronics,Audio,110.00,90.00,35,10,1,SUP003\nP010,Office Chair,Furniture,Seating,180.00,180.00,12,3,0,SUP002\nP011,,Electronics,Peripherals,45.00,110.00,50,10,1,SUP001\nP012,Docking Station,Electronics,,95.00,249.99,20,5,1,SUP003\nP013,Mousepad XL,Electronics,Peripherals,8.00,24.99,-5,20,1,SUP001\nP014,Monitor Arm,Furniture,Accessories,55.00,139.99,18,5,1,\nP015,USB Drive 256GB,Electronics,Storage,12.00,0.00,200,30,1,SUP001\n",
  "04_Employees.csv": "EmployeeID,FullName,Department,ManagerID,HireDate,Salary,Grade,Email,IsActive\nE001,Sarah Thompson,Sales,E010,2018-03-01,55000,L3,sarah.thompson@corp.com,1\nE002,Rajiv Menon,Technology,E011,2019-07-15,72000,L4,rajiv.menon@corp.com,1\nE003,Lisa Park,HR,E012,2017-01-10,48000,L2,lisa.park@corp.com,1\nE004,James Okafor,Finance,E010,2020-05-20,61000,L3,james.okafor@corp.com,1\nE005,Nadia Costa,Sales,E010,2021-09-01,42000,L2,nadia.costa@corp.com,1\nE006,Wei Zhang,Technology,E011,2016-11-30,88000,L5,wei.zhang@corp.com,1\nE007,Maria Gonzalez,Marketing,E013,2022-02-14,38000,L2,,1\nE008,Tom Harrington,Finance,E010,2015-06-01,95000,L5,tom.harrington@corp.com,0\nE009,Priya Iyer,HR,E012,2023-03-01,35000,L1,priya.iyer@corp.com,1\nE010,Robert Blake,Sales,,2010-04-01,120000,L7,robert.blake@corp.com,1\nE011,Claire Fontaine,Technology,,2012-08-01,135000,L7,claire.fontaine@corp.com,1\nE012,Karen Mills,HR,,2011-01-15,115000,L7,karen.mills@corp.com,1\nE013,Derek Chan,Marketing,,2013-09-01,118000,L6,derek.chan@corp.com,1\nE014,Alex Turner,Technology,E011,2024-01-01,-5000,L1,alex.turner@corp.com,1\nE015,Sara Thompson,Sales,E010,2018-03-01,55000,L3,sarah.thompson@corp.com,1\nE016,Fatima Al-Zahra,Finance,E010,2019-12-01,67000,L4,fatima.alzahra@corp.com,1\nE017,John Smith,Technology,E099,2021-06-15,58000,L3,john.smith@corp.com,1\nE018,Anita Rao,Marketing,E013,2025-03-01,40000,L2,anita.rao@corp.com,1\n",
  "05_Transactions.csv": "TransactionID,AccountID,TransactionDate,Amount,TransactionType,ReferenceNo,Status,ProcessedBy\nTXN0001,ACC001,2024-03-01,1500.00,Credit,REF10001,Completed,E001\nTXN0002,ACC002,2024-03-01,200.50,Debit,REF10002,Completed,E001\nTXN0003,ACC001,2024-03-02,300.00,Debit,REF10003,Completed,E002\nTXN0004,ACC003,2024-03-03,5000.00,Credit,REF10004,Completed,E003\nTXN0005,ACC002,2024-03-05,-150.00,Debit,REF10005,Completed,E001\nTXN0006,ACC004,2024-03-07,750.00,Credit,REF10006,Pending,E002\nTXN0007,ACC001,2024-03-07,750.00,Credit,REF10006,Pending,E002\nTXN0008,ACC005,2024-03-08,12000.00,Credit,REF10008,Completed,E004\nTXN0009,ACC006,2024-03-10,80.00,Debit,REF10009,Completed,E001\nTXN0010,ACC003,2024-03-12,5000.00,Credit,REF10010,Completed,E003\nTXN0011,ACC007,,450.00,Debit,REF10011,Completed,E002\nTXN0012,ACC008,2024-03-14,0.00,Credit,REF10012,Completed,E005\nTXN0013,ACC009,2024-03-15,99999999.99,Debit,REF10013,Flagged,E001\nTXN0014,ACC010,2024-03-16,320.00,Transfer,REF10014,Completed,E003\nTXN0015,ACC001,2024-03-17,320.00,Transfer,REF10014,Completed,E003\nTXN0016,ACC002,2024-03-18,180.00,Debit,REF10016,Completed,E099\nTXN0017,ACC003,2024-03-19,2200.00,Credit,,Completed,E004\nTXN0018,ACC004,2024-03-20,550.00,Debit,REF10018,Failed,E002\nTXN0019,ACC005,2024-03-21,1100.00,Credit,REF10019,Completed,E001\nTXN0020,ACC006,2024-03-22,400.00,Debit,REF10020,Completed,E003\n",
  "06_SalesTargets.csv": "TargetID,EmployeeID,Year,Quarter,TargetAmount,ActualAmount,Region\nST001,E001,2024,Q1,50000,47200,South\nST002,E001,2024,Q2,55000,62100,South\nST003,E005,2024,Q1,35000,34800,North\nST004,E005,2024,Q2,38000,41500,North\nST005,E010,2024,Q1,200000,195000,All\nST006,E010,2024,Q2,210000,225000,All\nST007,E001,2024,Q1,50000,47200,South\nST008,E016,2024,Q1,45000,,West\nST009,E017,2024,Q1,40000,38000,East\nST010,E999,2024,Q1,30000,29000,North\nST011,E005,2024,Q3,40000,0,North\nST012,E001,2024,Q3,58000,-1000,South\nST013,E016,2024,Q2,48000,51000,West\nST014,E009,2024,Q1,30000,28500,Central\nST015,E009,2024,Q2,32000,33200,Central\n",
  "SCHEMA_REFERENCE.md": "# Schema Reference Card\n\nAll examples in the `methods/` folder refer to one or more of these six tables.\nEach CSV contains **intentional data quality issues** so validation queries produce real results.\n\n---\n\n## Table 1 \u2014 Customers\n\n**File:** `01_Customers.csv`\n\n| Column | Type | Notes |\n|---|---|---|\n| CustomerID | INT | Primary Key |\n| FirstName | VARCHAR(50) | Required |\n| LastName | VARCHAR(50) | Required |\n| Email | VARCHAR(100) | Required, must be unique, must contain @ |\n| Phone | VARCHAR(20) | Optional |\n| DOB | DATE | Date of Birth \u2014 must be in the past, customer must be 18+ |\n| Country | VARCHAR(50) | Required |\n| PostalCode | VARCHAR(10) | Required |\n| CreatedDate | DATE | System date of record creation |\n| IsActive | BIT | 1 = Active, 0 = Inactive |\n\n**Known Issues Embedded:**\n- Row 3: Email is blank (NULL)\n- Row 4: Phone is NULL\n- Row 5: DOB is NULL\n- Row 7: Email is not a valid format (missing @domain)\n- Row 8: Customer DOB = 2005 \u2014 may be under 18 depending on check date\n- Row 11: Duplicate of Row 1 (same name, email, phone, DOB)\n- Row 15: DOB = 2010 \u2014 customer is a minor (age ~14)\n- Row 16: All fields populated with \"N/A\" placeholder strings\n- Row 19: Phone = 99999999999 \u2014 obviously fake/placeholder\n\n---\n\n## Table 2 \u2014 Orders\n\n**File:** `02_Orders.csv`\n\n| Column | Type | Notes |\n|---|---|---|\n| OrderID | INT | Primary Key |\n| CustomerID | INT | FK \u2192 Customers.CustomerID |\n| ProductID | VARCHAR(10) | FK \u2192 Products.ProductID |\n| OrderDate | DATE | Must be before or equal to ShipDate |\n| ShipDate | DATE | Can be NULL if not yet shipped |\n| Quantity | INT | Must be > 0 |\n| UnitPrice | DECIMAL(10,2) | Must be > 0 |\n| TotalAmount | DECIMAL(10,2) | Should equal Quantity \u00d7 UnitPrice \u00d7 (1 - DiscountPct/100) |\n| Status | VARCHAR(20) | Allowed: Pending, Processing, Shipped, Delivered, Returned, Cancelled |\n| DiscountPct | DECIMAL(5,2) | 0 to 100 |\n\n**Known Issues Embedded:**\n- Row 4 (OrderID 1004): CustomerID 99 does not exist in Customers\n- Row 6 (OrderID 1006): ShipDate (Jan 19) is BEFORE OrderDate (Jan 22)\n- Row 7 (OrderID 1007): Quantity = 0\n- Row 5 (OrderID 1005): TotalAmount 350.00 is wrong (should be 359.98 at 10% discount)\n- Row 19 (OrderID 1019): OrderDate = 2023-03-01 \u2014 a year in the past (likely data entry error)\n- Row 20 (OrderID 1020): UnitPrice and TotalAmount are negative\n\n---\n\n## Table 3 \u2014 Products\n\n**File:** `03_Products.csv`\n\n| Column | Type | Notes |\n|---|---|---|\n| ProductID | VARCHAR(10) | Primary Key |\n| ProductName | VARCHAR(100) | Required |\n| Category | VARCHAR(50) | Required \u2014 Electronics or Furniture |\n| SubCategory | VARCHAR(50) | Required |\n| UnitCost | DECIMAL(10,2) | Must be > 0 |\n| SellingPrice | DECIMAL(10,2) | Must be > UnitCost (gross margin > 0) |\n| StockQty | INT | Cannot be negative |\n| MinStockLevel | INT | Reorder threshold |\n| IsActive | BIT | 1 = Active, 0 = Discontinued |\n| SupplierID | VARCHAR(10) | FK \u2192 Supplier table |\n\n**Known Issues Embedded:**\n- P009: SellingPrice (90.00) < UnitCost (110.00) \u2014 selling at a loss\n- P010: SellingPrice = UnitCost = 180.00 \u2014 zero margin\n- P011: ProductName is NULL\n- P012: SubCategory is NULL\n- P013: StockQty = -5 (impossible negative stock)\n- P014: SupplierID is NULL\n- P015: SellingPrice = 0.00\n\n---\n\n## Table 4 \u2014 Employees\n\n**File:** `04_Employees.csv`\n\n| Column | Type | Notes |\n|---|---|---|\n| EmployeeID | VARCHAR(10) | Primary Key |\n| FullName | VARCHAR(100) | Required |\n| Department | VARCHAR(50) | Must be: Sales, Technology, HR, Finance, Marketing |\n| ManagerID | VARCHAR(10) | FK \u2192 Employees.EmployeeID (self-referencing); NULL for top-level managers |\n| HireDate | DATE | Cannot be in the future |\n| Salary | DECIMAL(10,2) | Must be > 0 |\n| Grade | VARCHAR(5) | L1 through L7 |\n| Email | VARCHAR(100) | Required, unique |\n| IsActive | BIT | 1 = Active, 0 = Left company |\n\n**Known Issues Embedded:**\n- E007: Email is NULL\n- E014: Salary = -5000 (negative salary)\n- E015: Near-duplicate of E001 (\"Sara\" vs \"Sarah\" Thompson \u2014 same email)\n- E017: ManagerID = E099 \u2014 manager does not exist\n- E018: HireDate = 2025-03-01 \u2014 future hire date (if current date < 2025-03-01)\n\n---\n\n## Table 5 \u2014 Transactions\n\n**File:** `05_Transactions.csv`\n\n| Column | Type | Notes |\n|---|---|---|\n| TransactionID | VARCHAR(10) | Primary Key |\n| AccountID | VARCHAR(10) | Account reference |\n| TransactionDate | DATE | Required |\n| Amount | DECIMAL(12,2) | Debit amounts should be positive (sign handled by TransactionType) |\n| TransactionType | VARCHAR(20) | Credit, Debit, or Transfer |\n| ReferenceNo | VARCHAR(20) | Should be unique per transaction |\n| Status | VARCHAR(20) | Completed, Pending, Failed, Flagged |\n| ProcessedBy | VARCHAR(10) | FK \u2192 Employees.EmployeeID |\n\n**Known Issues Embedded:**\n- TXN0006 and TXN0007: Same ReferenceNo (REF10006) \u2014 possible duplicate\n- TXN0011: TransactionDate is NULL\n- TXN0012: Amount = 0.00\n- TXN0013: Amount = 99,999,999.99 \u2014 statistical outlier / suspicious\n- TXN0014 and TXN0015: Same ReferenceNo (REF10014) \u2014 same reference, two accounts\n- TXN0016: ProcessedBy = E099 \u2014 employee does not exist\n- TXN0017: ReferenceNo is NULL\n- TXN0005: Amount = -150.00 (negative debit amount \u2014 sign error)\n\n---\n\n## Table 6 \u2014 SalesTargets\n\n**File:** `06_SalesTargets.csv`\n\n| Column | Type | Notes |\n|---|---|---|\n| TargetID | INT | Primary Key |\n| EmployeeID | VARCHAR(10) | FK \u2192 Employees.EmployeeID |\n| Year | INT | 4-digit year |\n| Quarter | VARCHAR(5) | Q1, Q2, Q3, Q4 |\n| TargetAmount | DECIMAL(10,2) | Must be > 0 |\n| ActualAmount | DECIMAL(10,2) | Can be NULL if period not yet closed |\n| Region | VARCHAR(50) | Sales region |\n\n**Known Issues Embedded:**\n- ST001 and ST007: Exact duplicate rows (E001, 2024, Q1)\n- ST008: ActualAmount is NULL \u2014 period may not be closed\n- ST010: EmployeeID E999 \u2014 does not exist in Employees\n- ST011: ActualAmount = 0 \u2014 target recorded with no sales at all\n- ST012: ActualAmount = -1000 \u2014 negative actual sales figure\n"
}
